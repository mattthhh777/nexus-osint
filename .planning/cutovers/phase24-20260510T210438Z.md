---
phase: 24-postgres-cutover
timestamp: 2026-05-10T21:04:38Z
status: complete
---

# Phase 24 Cutover Runbook Evidence

## Preflight

- User approval: `ok do`.
- Caveat accepted: dirty VPS worktree and no discovered DBM-46 staging rollback proof.
- Remote git SHA before cutover: `12936928810044dfa7cb2e27b6ec8979eec00c06`.
- Postgres health: `pg_isready` accepted connections.
- Migration assets synced: `scripts/port_searches.py`, `alembic.ini`, `migrations/`.

## Freeze + Backup

- `READ_ONLY_MODE=true` set in VPS `.env`.
- Nexus restarted for write freeze.
- SQLite WAL checkpoint: `PRAGMA wal_checkpoint(TRUNCATE)` -> `(0, 0, 0)`.
- Snapshot: `/app/data/audit.db.pre-pg-20260510T210438Z`.
- Snapshot permissions: `-r--------`.
- Snapshot `searches` count: `19`.
- Image tag: `nexus-osint-nexus:pre-pg-backup-20260510T210438Z`.

## Migration

- Alembic: `upgrade head` completed.
- Port command result: `PORT_SEARCHES_OK rows=19 elapsed_s=0.060`.
- Sequence reset: not applicable; schema uses UUID primary keys, no serial sequences.

## Runtime Flip

- VPS `.env` updated with encoded `DATABASE_URL=postgresql+asyncpg://...`.
- Nexus recreated with Postgres runtime.
- Compose command bug fixed so Uvicorn binds `0.0.0.0`.

## Smoke Evidence

- Internal `/health`: healthy with Postgres pool:
  - `db.started=true`
  - `db.size=2`
  - `db.idle_size=2`
  - `active_tasks=0`
  - `rss_mb=77.2`
- Public `https://nexusosint.uk/health`: healthy.
- Public `/`: HTTP `200`.
- Public `/admin`: HTTP `403` without admin cookie, expected.
- Postgres row count: `19`.
- Docker stats:
  - `nexus-osint 58.13MiB / 2.441GiB`
  - `nexus-postgres 36.17MiB / 768MiB`
  - `nexus-redis 3.297MiB`

## Rollback Anchor

- Restore target: `/app/data/audit.db.pre-pg-20260510T210438Z`.
- Image tag: `nexus-osint-nexus:pre-pg-backup-20260510T210438Z`.
- Important caveat: host code was synced forward during the cutover. A rollback must
  restore both image and matching pre-cutover host code, or keep the rebuilt image
  that includes `asyncpg` while removing `DATABASE_URL`.

