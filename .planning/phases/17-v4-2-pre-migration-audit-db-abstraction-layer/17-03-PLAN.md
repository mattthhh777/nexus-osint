---
phase: 17-v4-2-pre-migration-audit-db-abstraction-layer
plan: 03
type: execute
wave: 3
depends_on: ["17-02"]
files_modified:
  - api/deps.py
  - api/services/search_service.py
  - api/services/auth_service.py
  - api/routes/admin.py
  - api/main.py
autonomous: false
requirements: [DBM-02]
requirements_addressed: [DBM-02]
gap_closure: false

must_haves:
  truths:
    - "Only api/db.py imports aiosqlite — no other module references the driver"
    - "Call sites that previously caught aiosqlite.Error / aiosqlite.OperationalError now catch DatabaseError"
    - "17-01 canonical audit grep result is unchanged after the refactor (no SQL semantics changed)"
    - "Test suite stays green; smoke endpoints /health and /admin/dashboard respond OK"
  artifacts:
    - path: "api/deps.py"
      provides: "DB-error-aware dependency wiring without aiosqlite import"
      contains: "DatabaseError"
    - path: "api/services/search_service.py"
      provides: "DB-error-aware search service without aiosqlite import"
      contains: "DatabaseError"
    - path: "api/routes/admin.py"
      provides: "DB-error-aware admin routes without aiosqlite import"
      contains: "DatabaseError"
    - path: "api/main.py"
      provides: "Main app without unused aiosqlite import"
  key_links:
    - from: "api/deps.py, api/services/search_service.py, api/routes/admin.py"
      to: "api/db.DatabaseError"
      via: "from api.db import DatabaseError"
      pattern: "from api.db import .*DatabaseError"
    - from: "all api/ modules except api/db.py"
      to: "no aiosqlite reference"
      via: "removed imports + replaced except clauses"
      pattern: "aiosqlite"
---

<objective>
Contain `aiosqlite` to `api/db.py`. Every other module imports `DatabaseError` (added in 17-02) and uses it in `except` clauses. After this plan, Phase 21's driver swap is mechanical: only `api/db.py` changes.

Purpose: DBM-02 closure — the abstraction is real only if no caller bypasses it.
Output: 5 file edits, full suite green, manual smoke verification of /health and /admin/dashboard.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-CONTEXT.md
@.planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-02-SUMMARY.md
@api/db.py
@api/deps.py
@api/services/search_service.py
@api/services/auth_service.py
@api/routes/admin.py
@api/main.py

<interfaces>
<!-- Public surface from 17-02, used by this plan: -->

From api/db.py:
- class DatabaseError(Exception)
- DatabaseManager methods (canonical): fetch_one, fetch_all, fetch_stream, execute, execute_nowait, transaction
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Refactor api/deps.py — drop aiosqlite, catch DatabaseError</name>
  <read_first>
    - .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-CONTEXT.md (table of file changes)
    - api/deps.py (current import + line 113 except clause)
    - api/db.py (DatabaseError export)
  </read_first>
  <files>api/deps.py</files>
  <action>
    1) Remove the `import aiosqlite` line.
    2) Add `from api.db import DatabaseError` (group with existing api.db import if one exists).
    3) Replace `except (aiosqlite.Error, ...)` (around line 113) with `except (DatabaseError, OSError, ValueError, RuntimeError)`. Preserve every other exception type already in the tuple — only swap `aiosqlite.Error` for `DatabaseError`.
    4) Do not modify any business logic. Pure import/except refactor.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if (Select-String -Path api/deps.py -Pattern 'aiosqlite' -Quiet) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "aiosqlite" api/deps.py` returns 0 matches.
    - `grep -n "DatabaseError" api/deps.py` returns >=1 match.
    - `grep -nE "except\s*\([^)]*DatabaseError" api/deps.py` returns >=1 match.
    - `python -c "import api.deps"` exits 0.
  </acceptance_criteria>
  <done>api/deps.py is aiosqlite-free; uses DatabaseError in except tuple.</done>
</task>

<task type="auto">
  <name>Task 2: Refactor api/services/search_service.py + api/services/auth_service.py</name>
  <read_first>
    - api/services/search_service.py (line 355 except clause; rowid fix from 17-01 already applied)
    - api/services/auth_service.py (any db.write calls — optional rename to db.execute_nowait per CONTEXT.md)
    - api/db.py (DatabaseError + execute_nowait alias)
  </read_first>
  <files>
    api/services/search_service.py
    api/services/auth_service.py
  </files>
  <action>
    In `api/services/search_service.py`:
    1) Remove `import aiosqlite`.
    2) Add `from api.db import DatabaseError`.
    3) Replace `except (httpx.HTTPError, aiosqlite.Error, ...)` (around line 355) with `except (httpx.HTTPError, DatabaseError, ...)` — preserve all other exception types in the tuple, swap ONLY `aiosqlite.Error` for `DatabaseError`.
    4) Verify the rowid fix from 17-01 is still in place (`DELETE FROM quota_log WHERE id NOT IN ...`). If accidentally reverted, restore.

    In `api/services/auth_service.py` (style consistency — optional but do it):
    5) Replace any `db.write(` call with `db.execute_nowait(`. Replace any `db.write_await(` with `db.execute(`. Pure rename — same method (aliases are identity-preserving from 17-02).
    6) If `auth_service.py` has no aiosqlite import or aiosqlite.Error catch, leave imports alone. If it does, apply same pattern as steps 1-3.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Select-String -Path api/services/search_service.py -Pattern 'aiosqlite' -Quiet) -or (Select-String -Path api/services/auth_service.py -Pattern 'aiosqlite' -Quiet)) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "aiosqlite" api/services/search_service.py` returns 0 matches.
    - `grep -n "aiosqlite" api/services/auth_service.py` returns 0 matches.
    - `grep -n "DatabaseError" api/services/search_service.py` returns >=1 match.
    - `grep -n "DELETE FROM quota_log WHERE id NOT IN" api/services/search_service.py` returns 1 match (17-01 fix preserved).
    - `python -c "import api.services.search_service, api.services.auth_service"` exits 0.
  </acceptance_criteria>
  <done>Both service modules are aiosqlite-free; rowid fix preserved; auth_service uses canonical method names.</done>
</task>

<task type="auto">
  <name>Task 3: Refactor api/routes/admin.py + api/main.py</name>
  <read_first>
    - api/routes/admin.py (lines 69, 100 — aiosqlite.OperationalError catches)
    - api/main.py (line 42 aiosqlite import; line 8 comment)
    - api/db.py (DatabaseError)
  </read_first>
  <files>
    api/routes/admin.py
    api/main.py
  </files>
  <action>
    In `api/routes/admin.py`:
    1) Remove `import aiosqlite`.
    2) Add `from api.db import DatabaseError`.
    3) Replace BOTH `except aiosqlite.OperationalError` (around lines 69 and 100) with `except DatabaseError`. If the original except branch had specific OperationalError handling (e.g. "database is locked" message check), preserve the message-inspection logic — DatabaseError wraps the original message via str(e).

    In `api/main.py`:
    4) Remove `import aiosqlite` line (line 42 if present and unused). If used elsewhere in the file, replace usage with the appropriate api.db abstraction call BEFORE removing the import. Verify with `grep -n "aiosqlite" api/main.py` — must return 0 after edit.
    5) Update line 8 comment if it references aiosqlite — change to reflect that DB driver details live behind api/db.py. Example: `# DB driver details live in api/db.py (repository abstraction — Phase 17 v4.2).`
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Select-String -Path api/routes/admin.py -Pattern 'aiosqlite' -Quiet) -or (Select-String -Path api/main.py -Pattern 'aiosqlite' -Quiet)) { exit 1 } else { exit 0 }"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -n "aiosqlite" api/routes/admin.py` returns 0 matches.
    - `grep -n "aiosqlite" api/main.py` returns 0 matches.
    - `grep -n "DatabaseError" api/routes/admin.py` returns >=1 match.
    - `grep -nE "except\s+DatabaseError" api/routes/admin.py` returns >=2 matches (both former OperationalError sites).
    - `python -c "import api.main"` exits 0.
  </acceptance_criteria>
  <done>admin.py and main.py are aiosqlite-free; DatabaseError used in admin except clauses.</done>
</task>

<task type="auto">
  <name>Task 4: Containment verification + 17-01 audit re-run</name>
  <read_first>
    - .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-CONTEXT.md (DoD list)
    - .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/SQL_INVENTORY.md (baseline from 17-01)
  </read_first>
  <files>
    (verification only — no file edits unless a failure forces a fix)
  </files>
  <action>
    1) Run: `grep -rn "aiosqlite" api/ --include="*.py"`. Expected: ONLY api/db.py lines. If any other file appears, return to Tasks 1-3 and finish containment.
    2) Re-run 17-01 canonical audit grep: `grep -rnE "AUTOINCREMENT|INSERT OR REPLACE|datetime\(|strftime\(|\browid\b" api/ scripts/ tests/`. Expected: identical to 17-01 result (only api/db.py:131 AUTOINCREMENT). No drift.
    3) Run `pytest -q`. Expected: exit 0, full suite green.
    4) Confirm `tests/test_db_abstraction.py` (from 17-02) and `tests/test_quota_log_rowid_fix.py` (from 17-01) still pass.
  </action>
  <verify>
    <automated>pytest -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -rn "aiosqlite" api/ --include="*.py"` matches lines ONLY in api/db.py.
    - `grep -rnE "AUTOINCREMENT|INSERT OR REPLACE|datetime\(|strftime\(|\browid\b" api/ scripts/ tests/` returns ONLY api/db.py:131.
    - `pytest -q` exits 0.
  </acceptance_criteria>
  <done>aiosqlite is contained; audit baseline unchanged; suite green.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 5: Smoke /health + /admin/dashboard</name>
  <read_first>
    - .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-CONTEXT.md (smoke command list)
    - api/main.py (post-refactor)
  </read_first>
  <what-built>
    Refactored 5 modules to remove aiosqlite imports and replace except clauses with DatabaseError. Containment verified by grep + full pytest suite.
  </what-built>
  <how-to-verify>
    1. Start the app locally: `uvicorn api.main:app --port 8000`
    2. Hit `http://localhost:8000/health` — expect HTTP 200 with JSON body containing memory_used_mb, memory_pct, cpu_pct, active_agents.
    3. Authenticate as admin (existing flow), then hit `http://localhost:8000/admin/dashboard` — expect HTTP 200 and the dashboard page renders without 500.
    4. Trigger one search via the UI — expect normal completion (no DB-related errors in logs).
    5. Inspect docker logs / stdout for any unexpected DatabaseError traceback (a clean run produces none).

    If any step fails, capture the traceback and resume by reporting which file's except clause is the likely culprit.
  </how-to-verify>
  <acceptance_criteria>
    - User confirms /health returns 200 with expected JSON shape.
    - User confirms /admin/dashboard returns 200 and renders.
    - User confirms one end-to-end search completes without DB errors in logs.
  </acceptance_criteria>
  <resume-signal>Type "approved" or describe issues (file + traceback).</resume-signal>
</task>

</tasks>

<verification>
- `grep -rn "aiosqlite" api/ --include="*.py"` matches only api/db.py.
- `grep -rnE "AUTOINCREMENT|INSERT OR REPLACE|datetime\(|strftime\(|\browid\b" api/ scripts/ tests/` returns only api/db.py:131.
- `pytest -q` green.
- Smoke /health + /admin/dashboard green (Task 5).
</verification>

<success_criteria>
- DBM-02 closed: only api/db.py touches aiosqlite; all callers use DatabaseError.
- 17-01 audit baseline preserved (no semantic SQL change in this plan).
- Commit: `refactor(v4.2): contain aiosqlite to api/db.py + use DatabaseError at call sites`
</success_criteria>

<output>
After completion, create `.planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-03-SUMMARY.md` with: list of 5 files refactored, before/after grep counts for `aiosqlite`, smoke result, full-suite pass count, confirmation that Phase 21 driver swap surface = api/db.py only.
</output>
