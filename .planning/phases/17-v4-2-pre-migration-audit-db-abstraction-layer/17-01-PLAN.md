---
phase: 17-v4-2-pre-migration-audit-db-abstraction-layer
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/SQL_INVENTORY.md
  - .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/PLACEHOLDER_MAP.md
  - api/services/search_service.py
  - tests/test_quota_log_rowid_fix.py
autonomous: true
requirements: [DBM-01, DBM-03, DBM-04]
requirements_addressed: [DBM-01, DBM-03, DBM-04]
gap_closure: false

must_haves:
  truths:
    - "Every raw SQL statement in api/, scripts/, tests/ is inventoried in SQL_INVENTORY.md"
    - "Every '?' placeholder site has a documented '$N' rewrite target in PLACEHOLDER_MAP.md"
    - "quota_log retention DELETE no longer relies on SQLite-only 'rowid' (uses 'id' column)"
    - "Canonical audit grep returns only api/db.py:131 (AUTOINCREMENT in DDL — Phase 19 deferral)"
  artifacts:
    - path: ".planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/SQL_INVENTORY.md"
      provides: "Inventory of every raw SQL statement"
      contains: "File:Line | Statement | Type | Table | Placeholders | Migration notes"
    - path: ".planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/PLACEHOLDER_MAP.md"
      provides: "? -> $N placeholder rewrite map"
      contains: "File:Line | SQL with ? | Target SQL with $N | Refactor location"
    - path: "api/services/search_service.py"
      provides: "rowid-free quota_log DELETE using 'id' PK"
      contains: "DELETE FROM quota_log WHERE id NOT IN"
  key_links:
    - from: "SQL_INVENTORY.md"
      to: "api/, scripts/, tests/"
      via: "exhaustive grep coverage"
      pattern: "(SELECT |INSERT |UPDATE |DELETE |CREATE |DROP |ALTER )"
    - from: "PLACEHOLDER_MAP.md"
      to: "every '?' usage in app code"
      via: "1:1 mapping by File:Line"
      pattern: "\\?"
---

<objective>
Produce the v4.2 SQL audit baseline: complete inventory of raw SQL, complete `?`->`$N` placeholder rewrite map, and the only in-place SQL fix that is strictly required before driver swap (the SQLite-only `rowid` violation in `quota_log` retention).

Purpose: DBM-01/DBM-03/DBM-04 — make Phase 21 driver swap mechanical (no surprises) by knowing every SQL site in the codebase and how each maps to PostgreSQL syntax.
Output: SQL_INVENTORY.md, PLACEHOLDER_MAP.md, search_service.py rowid->id fix, regression test.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-CONTEXT.md
@.planning/research/PITFALLS.md
@api/services/search_service.py
@api/db.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Generate SQL_INVENTORY.md and PLACEHOLDER_MAP.md (DBM-03, DBM-04, DBM-01 baseline)</name>
  <read_first>
    - .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-CONTEXT.md (locked decisions, canonical greps)
    - .planning/research/PITFALLS.md (Pitfall 1 dialect drift)
    - api/db.py (DDL site at line 131 — AUTOINCREMENT — DOCUMENT ONLY, do NOT modify)
    - api/services/search_service.py (rowid violation site lines 56-57)
  </read_first>
  <files>
    .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/SQL_INVENTORY.md
    .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/PLACEHOLDER_MAP.md
  </files>
  <action>
    Run the two canonical greps from CONTEXT.md verbatim:

    ```
    grep -rnE "AUTOINCREMENT|INSERT OR REPLACE|datetime\(|strftime\(|\browid\b|\.fetchone\(|\.fetchall\(" api/ scripts/ tests/
    grep -rnE "(SELECT |INSERT |UPDATE |DELETE |CREATE |DROP |ALTER )" api/ scripts/
    ```

    Build SQL_INVENTORY.md as a markdown table with EXACT columns:
    `File:Line | Statement | Type | Table | Placeholders | Migration notes`

    - Statement: SQL trimmed to <=120 chars (preserve semantics).
    - Type: SELECT / INSERT / UPDATE / DELETE / CREATE / DROP / ALTER / DDL / PRAGMA.
    - Table: target table name (or `n/a` for PRAGMA).
    - Placeholders: count of `?` in the statement.
    - Migration notes: PG-impact flags. Use these literal tags:
      - `DIALECT:AUTOINCREMENT` (only `api/db.py:131`) — note "Phase 19 — UUID-all schema rewrite".
      - `DIALECT:INSERT_OR_REPLACE` (none expected — record any found).
      - `DIALECT:DATETIME_FN` (any `datetime(` or `strftime(`).
      - `DIALECT:ROWID` (search_service.py:56-57 — fixed in Task 2).
      - `OK` for portable statements.

    Build PLACEHOLDER_MAP.md as a markdown table with EXACT columns:
    `File:Line | SQL with ? | Target SQL with $N | Refactor location`

    - Cover 100% of `?` sites returned by grep (every line in api/, scripts/, tests/ where SQL contains `?`).
    - Number `?` left-to-right as `$1, $2, $3, ...`.
    - Refactor location: "Phase 21 (api/db.py driver swap)" for sites going through abstraction; "Phase 21 + call-site rewrite" if SQL string is built outside abstraction.

    Both files MUST be saved to `.planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/`.

    DO NOT modify `api/db.py:131` (AUTOINCREMENT) — it is deferred to Phase 19 per CONTEXT.md `<deferred>`. Document only.
  </action>
  <verify>
    <automated>powershell -NoProfile -Command "if ((Test-Path .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/SQL_INVENTORY.md) -and (Test-Path .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/PLACEHOLDER_MAP.md)) { exit 0 } else { exit 1 }"</automated>
  </verify>
  <acceptance_criteria>
    - SQL_INVENTORY.md exists and contains the literal header `File:Line | Statement | Type | Table | Placeholders | Migration notes`.
    - PLACEHOLDER_MAP.md exists and contains the literal header `File:Line | SQL with ? | Target SQL with $N | Refactor location`.
    - SQL_INVENTORY.md tags `api/db.py:131` with `DIALECT:AUTOINCREMENT` and notes "Phase 19".
    - SQL_INVENTORY.md tags `api/services/search_service.py:56` (or :57) with `DIALECT:ROWID`.
    - Every line emitted by `grep -rnE "(SELECT |INSERT |UPDATE |DELETE |CREATE |DROP |ALTER )" api/ scripts/` is represented in SQL_INVENTORY.md (count match).
    - Every line emitted by `grep -rnE "\?" api/ scripts/ tests/` that is INSIDE a SQL string appears in PLACEHOLDER_MAP.md.
  </acceptance_criteria>
  <done>
    Both audit deliverables exist on disk with the exact column schemas; AUTOINCREMENT is documented (not modified); ROWID violation is flagged for Task 2.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace SQLite-only `rowid` with `id` PK in quota_log retention (DBM-01)</name>
  <read_first>
    - .planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-CONTEXT.md (R-17.3 mitigation, "200 inserts -> 100 newest remain" test spec)
    - api/services/search_service.py (current DELETE statement at lines 56-57)
    - api/db.py (quota_log DDL — confirm `id` PK column exists)
    - tests/conftest.py (existing fixtures for in-memory DB)
  </read_first>
  <files>
    api/services/search_service.py
    tests/test_quota_log_rowid_fix.py
  </files>
  <behavior>
    - Test 1: Insert 200 rows into quota_log with monotonically increasing `created_at`; run new DELETE; assert exactly 100 rows remain.
    - Test 2: After delete, assert `min(id)` of remaining rows is strictly greater than `max(id)` of any deleted row (i.e. the 100 newest by id were preserved).
    - Test 3: Re-running the DELETE on the now-100-row table is a no-op (still 100 rows remain).
  </behavior>
  <action>
    1) RED — write `tests/test_quota_log_rowid_fix.py` first. Use pytest-asyncio + the existing in-memory DB fixture style from `tests/conftest.py`. Three test functions matching the behaviors above. Confirm tests FAIL against current code.

    2) GREEN — Edit `api/services/search_service.py` lines 56-57. Change:

       ```sql
       DELETE FROM quota_log
       WHERE rowid NOT IN (SELECT rowid FROM quota_log ORDER BY created_at DESC LIMIT 100)
       ```

       to:

       ```sql
       DELETE FROM quota_log
       WHERE id NOT IN (SELECT id FROM quota_log ORDER BY id DESC LIMIT 100)
       ```

       Rationale (preserve as inline comment): `rowid` is SQLite-specific; `id` is the explicit PK present in the schema, equivalent under monotonic insert order, and PG-compatible (BIGSERIAL / UUID).

    3) Confirm tests pass.

    4) Update SQL_INVENTORY.md: change the `DIALECT:ROWID` tag for this line to `OK` (post-fix). The grep `grep -nE "\browid\b" api/services/search_service.py` MUST return zero matches.

    No other behavior changes. Do not touch the surrounding except clauses (those are 17-03's job).
  </action>
  <verify>
    <automated>pytest tests/test_quota_log_rowid_fix.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "\browid\b" api/services/search_service.py` returns zero matches.
    - `grep -nE "DELETE FROM quota_log WHERE id NOT IN" api/services/search_service.py` returns exactly 1 match.
    - `pytest tests/test_quota_log_rowid_fix.py -q` exits 0 with 3 tests passing.
    - Canonical audit grep `grep -rnE "AUTOINCREMENT|INSERT OR REPLACE|datetime\(|strftime\(|\browid\b" api/ scripts/ tests/` returns ONLY `api/db.py:131` (AUTOINCREMENT, deferred).
    - Full existing suite `pytest -q` exits 0 (no regressions).
    - SQL_INVENTORY.md updated: search_service.py rowid line tag is now `OK`.
  </acceptance_criteria>
  <done>
    DBM-01 audit grep is clean except for the documented Phase-19-deferred AUTOINCREMENT site; new regression test pins the corrected behavior.
  </done>
</task>

</tasks>

<verification>
- `grep -rnE "AUTOINCREMENT|INSERT OR REPLACE|datetime\(|strftime\(|\browid\b" api/ scripts/ tests/` outputs exactly one line: `api/db.py:131:        ... AUTOINCREMENT ...`
- `pytest -q` green.
- SQL_INVENTORY.md and PLACEHOLDER_MAP.md exist with required column schemas.
</verification>

<success_criteria>
- DBM-01 satisfied: audit grep returns zero unmitigated dialect violations.
- DBM-03 satisfied: 100% of raw SQL statements inventoried.
- DBM-04 satisfied: 100% of `?` placeholder sites mapped to `$N`.
- Commit: `docs(v4.2): phase 17-01 SQL inventory + placeholder map + rowid fix`
</success_criteria>

<output>
After completion, create `.planning/phases/17-v4-2-pre-migration-audit-db-abstraction-layer/17-01-SUMMARY.md` with: counts (statements inventoried, `?` sites mapped), confirmation grep clean except db.py:131, link to SQL_INVENTORY.md and PLACEHOLDER_MAP.md.
</output>
