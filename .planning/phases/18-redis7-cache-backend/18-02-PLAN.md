---
phase: 18-redis7-cache-backend
plan: 02
type: execute
wave: 2
depends_on: ["18-01"]
files_modified:
  - api/services/search_service.py
  - api/routes/health.py
  - api/main.py
  - requirements.txt
  - tests/test_search_cache_redis.py
  - tests/test_health.py
autonomous: true
requirements: [CACHE-04, CACHE-05, CACHE-07, CACHE-09, CACHE-10]
requirements_addressed: [CACHE-04, CACHE-05, CACHE-07, CACHE-09, CACHE-10]

must_haves:
  truths:
    - "search_service no longer imports cachetools or exposes _api_cache"
    - "All search cache reads/writes use await cache_backend.get/set"
    - "OathNet cache values are JSON-safe DTOs and exclude raw_response plus nested raw payloads"
    - "/health gets cache stats from api.cache, not search_service internals"
    - "requirements.txt no longer includes cachetools"
  artifacts:
    - path: "api/services/search_service.py"
      provides: "Redis-backed search cache usage"
      contains: "await cache_backend.get("
    - path: "api/routes/health.py"
      provides: "Cache stats in health payload"
      contains: "\"cache\""
    - path: "tests/test_search_cache_redis.py"
      provides: "Cache hit/miss behavior tests"
      contains: "test_redis_cache_hit_skips_second_oathnet_call"
  key_links:
    - from: "api/services/search_service.py"
      to: "api/cache.py"
      via: "async cache backend contract"
      pattern: "cache_backend"
---

<objective>
Move actual search cache behavior from `cachetools.TTLCache` to Redis-backed async cache, then expose health stats and remove the old dependency.

Purpose: complete Redis7 replacement without changing search output semantics.
Output: migrated cache call sites, DTO serializers, health metrics, tests, `cachetools` removed.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@.planning/REQUIREMENTS.md
@.planning/phases/18-redis7-cache-backend/18-CONTEXT.md
@.planning/phases/18-redis7-cache-backend/18-01-SUMMARY.md
@api/cache.py
@api/services/search_service.py
@api/routes/health.py
@api/main.py
@modules/oathnet_client.py
@requirements.txt
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add DTO round-trip tests for OathNet cache values</name>
  <files>api/services/search_service.py, tests/test_search_cache_redis.py</files>
  <read_first>api/services/search_service.py, modules/oathnet_client.py, api/cache.py</read_first>
  <action>
    Create `tests/test_search_cache_redis.py` with DTO tests first:
    - Build an `OathnetResult` containing one `BreachRecord`, one `StealerRecord`, `OathnetMeta`, `holehe_domains`, non-empty `raw_response`, and non-empty nested `BreachRecord.raw` / `StealerRecord.raw`.
    - Include representative `extra_fields`, `data_types`, `domain`, `email` list, and `log` values so cached DTOs do not silently drop fields that affect rendering or future pagination.
    - Convert through the new cache DTO helper.
    - Assert DTO does not contain string `raw_response`.
    - Assert DTO does not contain nested raw payload keys named `raw`.
    - Reconstruct `OathnetResult`.
    - Assert reconstructed `breaches[0].email`, `breaches[0].extra_fields`, `stealers[0].username`, `stealers[0].email`, `meta.left_today`, and `holehe_domains` equal source values.

    Implement helpers in `api/services/search_service.py`:
    - `_oathnet_result_to_cache(result: OathnetResult) -> dict`
    - `_oathnet_result_from_cache(data: dict) -> OathnetResult`
    - `_drop_raw_payload_fields(value)` recursive helper or equivalent that removes `raw_response` and any key exactly named `raw`.

    Use dataclass-aware conversion. Do not use pickle. Do not include `raw_response` or nested `raw` payload fields.
  </action>
  <verify>pytest tests/test_search_cache_redis.py -q -k dto</verify>
  <acceptance_criteria>
    - `api/services/search_service.py` contains `_oathnet_result_to_cache`.
    - `api/services/search_service.py` contains `_oathnet_result_from_cache`.
    - `rg -n "pickle" api/services/search_service.py` returns no matches.
    - DTO test confirms `raw_response` and nested `raw` payloads are absent.
  </acceptance_criteria>
  <done>OathNet result values can safely round-trip through JSON cache payloads.</done>
</task>

<task type="auto">
  <name>Task 2: Migrate search_service cache calls to async cache backend</name>
  <files>api/services/search_service.py, requirements.txt</files>
  <read_first>api/services/search_service.py, api/cache.py, modules/oathnet_client.py, .planning/phases/18-redis7-cache-backend/18-CONTEXT.md</read_first>
  <action>
    Update `api/services/search_service.py`:
    - Remove `from cachetools import TTLCache`.
    - Remove `_api_cache: TTLCache = TTLCache(maxsize=200, ttl=300)`.
    - Import `cache_backend` from `api.cache`.
    - Keep `_cache_key(endpoint, query)` only if tests still need normalized key semantics; otherwise remove it and rely on `api.cache`.
    - Change `_get_cached(endpoint, query)` to async and return `await cache_backend.get(endpoint, query)`.
    - Change `_set_cached(endpoint, query, data)` to async and call `await cache_backend.set(endpoint, query, data)` after DTO conversion.
    - Update every cache call in `_stream_search` to `await _get_cached(...)` or `await _set_cached(...)`.
    - For breach and stealer results, store DTO dicts and reconstruct `OathnetResult` on get.
    - For holehe, discord, ip_info, steam, xbox, roblox values, store JSON-safe dict/list values directly.

    Update `requirements.txt`:
    - Remove `cachetools==5.5.0`.

    Preserve behavior:
    - Cache miss still calls OathNet/API.
    - Cache hit still skips OathNet/API.
    - Cache failure returns miss and search continues.
  </action>
  <verify>rg -n "TTLCache|cachetools|_api_cache" api requirements.txt</verify>
  <acceptance_criteria>
    - `rg -n "TTLCache|cachetools|_api_cache" api requirements.txt` returns no matches.
    - `api/services/search_service.py` contains `await cache_backend.get(`.
    - `api/services/search_service.py` contains `await cache_backend.set(`.
    - All `_get_cached(` call sites in `api/services/search_service.py` are preceded by `await`.
    - All `_set_cached(` call sites in `api/services/search_service.py` are preceded by `await`.
  </acceptance_criteria>
  <done>Old process-local TTLCache is removed from search_service and dependency list.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Prove Redis cache hit skips duplicate upstream call</name>
  <files>tests/test_search_cache_redis.py, api/services/search_service.py</files>
  <read_first>tests existing search-service tests, api/services/search_service.py, api/cache.py</read_first>
  <action>
    Extend `tests/test_search_cache_redis.py`:
    - Use a fake cache backend or `InMemoryCacheBackend`.
    - Patch `search_service.cache_backend` to the fake.
    - Patch `oathnet_client.search_breach` with an async fake that increments a counter and returns a valid `OathnetResult`.
    - Run the smallest search path that exercises breach cache twice for the same query, or call the cache wrapper around the same endpoint/query if full SSE setup is too expensive.
    - Test name must be `test_redis_cache_hit_skips_second_oathnet_call`.
    - Assert upstream counter is `1` after two same endpoint/query operations within TTL.
  </action>
  <verify>pytest tests/test_search_cache_redis.py -q</verify>
  <acceptance_criteria>
    - `tests/test_search_cache_redis.py` contains `test_redis_cache_hit_skips_second_oathnet_call`.
    - `pytest tests/test_search_cache_redis.py -q` exits 0.
  </acceptance_criteria>
  <done>Integration-level cache behavior proves duplicate call avoidance.</done>
</task>

<task type="auto">
  <name>Task 4: Move health cache stats to api.cache</name>
  <files>api/routes/health.py, api/main.py, tests/test_health.py</files>
  <read_first>api/routes/health.py, api/main.py, api/cache.py</read_first>
  <action>
    Update `api/routes/health.py`:
    - Remove `from api.services.search_service import _api_cache`.
    - Import `cache_backend` from `api.cache`.
    - In `/health`, call `await cache_backend.stats()` or use an async-safe stats method.
    - Add `payload["cache"]` dict with keys `backend`, `reachable`, `hits`, `misses`, `errors`, `entries`.
    - Set legacy `cache_entries` from stats entries when not `None`, else `0`.
    - In `/health/memory`, replace `cache_size`/`cache_maxsize` with `cache` stats dict. If keeping legacy fields for compatibility, set `cache_size` from entries or `0` and `cache_maxsize` to `None`.

    Update `api/main.py`:
    - Remove stale `_api_cache` import from `api.services.search_service`.

    Update or add tests:
    - `/health` response contains `cache.backend`.
    - `/health` response contains legacy `cache_entries`.
    - `api/routes/health.py` no longer imports `api.services.search_service`.
  </action>
  <verify>pytest tests/test_health.py -q</verify>
  <acceptance_criteria>
    - `api/routes/health.py` contains `payload["cache"]`.
    - `api/routes/health.py` does not contain `_api_cache`.
    - `api/main.py` does not contain `_api_cache`.
    - Health tests pass.
  </acceptance_criteria>
  <done>Health/memory cache metrics are backend-agnostic and no longer coupled to search_service internals.</done>
</task>

</tasks>

<verification>
- `pytest tests/test_cache_backend.py tests/test_search_cache_redis.py tests/test_health.py -q`
- `rg -n "TTLCache|cachetools|_api_cache" api requirements.txt` returns no matches.
- `rg -n "redis:7-alpine|CACHE_TTL_SECONDS|cache_backend|payload\\[\"cache\"\\]" docker-compose.yml api .env.example requirements.txt`
</verification>

<success_criteria>
- CACHE-04, CACHE-05, CACHE-07, CACHE-09, CACHE-10 satisfied.
- `cachetools` fully removed.
- Redis outage cannot turn a search into HTTP 500 through cache code.
- Summary file `.planning/phases/18-redis7-cache-backend/18-02-SUMMARY.md` created after execution.
</success_criteria>

<output>
After completion, create `.planning/phases/18-redis7-cache-backend/18-02-SUMMARY.md` with test results, grep proof that TTLCache is gone, and any Redis smoke command used.
</output>
