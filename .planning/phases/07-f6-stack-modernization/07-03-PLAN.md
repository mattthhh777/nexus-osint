---
phase: 07-f6-stack-modernization
plan: 03
type: execute
wave: 2
depends_on:
  - 07-01
  - 07-02
files_modified:
  - Dockerfile
autonomous: false
requirements:
  - F6-PY312
must_haves:
  truths:
    - "Dockerfile uses python:3.12-slim in both builder and runtime stages"
    - "Container builds successfully"
    - "All tests pass when run inside the 3.12 container"
    - "Docker image size remains < 250MB"
    - "No deprecation warnings from Python 3.12 in test output"
  artifacts:
    - path: "Dockerfile"
      provides: "Python 3.12-slim base image, multi-stage preserved"
      contains: "python:3.12-slim"
  key_links:
    - from: "Dockerfile builder stage"
      to: "Dockerfile runtime stage"
      via: "COPY --from=builder /install /usr/local"
      pattern: "COPY --from=builder"
---

<objective>
Upgrade Dockerfile base image from `python:3.11-slim` to `python:3.12-slim` (both stages) and certify the stack runs identically under 3.12.

Purpose: Final step of F6 Stack Modernization. Unblocks future dependencies that require 3.12 and eliminates the 3.11 end-of-life risk.
Output: Updated Dockerfile, test suite green under 3.12, image still < 250MB.

**GATE:** This plan must NOT start until Plans 07-01 and 07-02 are complete (test suite at 4 tests, DEPLOY.md exists, tenacity removed, FIND-16 closed).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-f6-stack-modernization/07-CONTEXT.md
@.planning/phases/07-f6-stack-modernization/07-01-SUMMARY.md
@.planning/phases/07-f6-stack-modernization/07-02-SUMMARY.md
@Dockerfile
@DEPLOY.md
@CLAUDE.md

**NOTE:** `Dockerfile` is listed as a protected file in CLAUDE.md. The user has explicitly authorized this change via Phase 07 CONTEXT D-01/D-02. The change is limited to the FROM lines only — no other modifications allowed in this plan.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Execute pre-upgrade snapshot per DEPLOY.md</name>
  <files></files>
  <read_first>
    - DEPLOY.md (Pre-Upgrade Snapshot section — follow verbatim)
  </read_first>
  <action>
    Execute the snapshot commands from DEPLOY.md "Pre-Upgrade Snapshot" section. Commands to run locally (VPS execution happens during actual deploy — not part of this plan):

    ```bash
    # 1. Freeze current deps
    pip freeze > requirements.lock.pre-python312.txt

    # 2. Tag current local image if it exists (skip gracefully if not built yet)
    docker image inspect nexus:latest >/dev/null 2>&1 && \
        docker tag nexus:latest nexus:pre-py312-backup || \
        echo "nexus:latest not present locally — skipping tag (will be done on VPS per DEPLOY.md)"
    ```

    Do NOT commit `requirements.lock.pre-python312.txt` to main. It lives on the feature branch only as evidence of the snapshot.
  </action>
  <verify>
    <automated>test -f requirements.lock.pre-python312.txt && test -s requirements.lock.pre-python312.txt</automated>
  </verify>
  <acceptance_criteria>
    - File `requirements.lock.pre-python312.txt` exists and is non-empty
    - File contains pinned versions (grep matches `==`): `grep -q "==" requirements.lock.pre-python312.txt`
  </acceptance_criteria>
  <done>Snapshot captured. Rollback source exists on disk.</done>
</task>

<task type="auto">
  <name>Task 2: Edit Dockerfile — upgrade both FROM lines to python:3.12-slim</name>
  <files>Dockerfile</files>
  <read_first>
    - Dockerfile (current content — confirm two FROM lines, both `python:3.11-slim`)
  </read_first>
  <action>
    Modify Dockerfile with exactly two textual changes:

    1. Line matching `FROM python:3.11-slim AS builder` -> `FROM python:3.12-slim AS builder`
    2. Line matching `FROM python:3.11-slim AS runtime` -> `FROM python:3.12-slim AS runtime`

    All other lines (WORKDIR, COPY, RUN, HEALTHCHECK, CMD, etc.) MUST remain byte-identical. Do NOT reorder, refactor, or "improve" the file. Do NOT pin to a digest yet — pinning is post-stabilization work (planner's discretion noted in CONTEXT, deferred).

    After editing, verify the diff is exactly 2 lines changed:
    ```bash
    git diff Dockerfile
    ```
  </action>
  <verify>
    <automated>test $(grep -c "python:3.12-slim" Dockerfile) -eq 2 && test $(grep -c "python:3.11-slim" Dockerfile) -eq 0</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "python:3.12-slim" Dockerfile` returns exactly `2`
    - `grep -c "python:3.11-slim" Dockerfile` returns exactly `0`
    - `grep -c "^FROM" Dockerfile` returns exactly `2` (multi-stage preserved)
    - `grep -q "AS builder" Dockerfile && grep -q "AS runtime" Dockerfile`
    - `git diff --stat Dockerfile` shows only 2 lines changed (2 insertions, 2 deletions)
  </acceptance_criteria>
  <done>Dockerfile base image upgraded, multi-stage structure preserved, diff is minimal and surgical.</done>
</task>

<task type="auto">
  <name>Task 3: Build image under 3.12, run full test suite inside it, verify size</name>
  <files></files>
  <read_first>
    - Dockerfile (post-edit)
    - DEPLOY.md "Upgrade Procedure" steps 3-5
  </read_first>
  <action>
    1. Build without cache to force fresh layer pull:
       ```bash
       docker compose build --no-cache nexus
       ```
       On failure: trigger rollback per DEPLOY.md and STOP.

    2. Check image size (must be < 250MB — hard constraint from CLAUDE.md):
       ```bash
       docker images nexus --format "{{.Repository}}:{{.Tag}} {{.Size}}"
       ```
       Record the numeric size. If >= 250MB: STOP, report, do not proceed to checkpoint.

    3. Run the full test suite INSIDE the new 3.12 container. This is the authoritative 3.12 compatibility check:
       ```bash
       docker compose run --rm nexus python -W error::DeprecationWarning -m pytest tests/ -q
       ```
       The `-W error::DeprecationWarning` flag upgrades deprecation warnings to errors — zero tolerance for 3.12 deprecations per D-01 gate.

    4. If tests fail: execute rollback procedure from DEPLOY.md immediately. Do NOT attempt to patch forward.

    5. Record the output in a scratch file for the summary:
       ```bash
       docker compose run --rm nexus python --version > .planning/phases/07-f6-stack-modernization/.py312-evidence.txt
       docker images nexus --format "{{.Size}}" >> .planning/phases/07-f6-stack-modernization/.py312-evidence.txt
       ```
  </action>
  <verify>
    <automated>docker compose run --rm nexus python --version 2>&1 | grep -q "3.12" && docker compose run --rm nexus python -W error::DeprecationWarning -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `docker compose run --rm nexus python --version` output contains literal "3.12"
    - `docker images nexus --format "{{.Size}}"` shows a value < 250MB (manual check, record in summary)
    - `docker compose run --rm nexus python -m pytest tests/ -q` exits 0
    - Zero DeprecationWarning errors in pytest output (enforced by `-W error::DeprecationWarning`)
    - `.planning/phases/07-f6-stack-modernization/.py312-evidence.txt` exists and contains "3.12"
  </acceptance_criteria>
  <done>Image builds on 3.12, size < 250MB, test suite green inside container, zero deprecation warnings.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 4: Human verification of 3.12 upgrade</name>
  <what-built>
    Dockerfile upgraded from python:3.11-slim to python:3.12-slim (both stages). Image rebuilt locally, all tests passing under 3.12 with zero deprecation warnings. Pre-upgrade snapshot captured in requirements.lock.pre-python312.txt.
  </what-built>
  <how-to-verify>
    1. Review the Dockerfile diff: `git diff Dockerfile` — must show exactly 2 line changes, both FROM statements.
    2. Confirm image size: `docker images nexus --format "{{.Repository}}:{{.Tag}}\t{{.Size}}"` — report the number. Acceptable if < 250MB.
    3. Run tests yourself: `docker compose run --rm nexus python -m pytest tests/ -v`
    4. Spot-check runtime: `docker compose up -d nexus && sleep 5 && curl -f http://localhost:8000/health && docker compose logs nexus | tail -30`
    5. Confirm the /health response shape is unchanged from Phase 06 baseline.
    6. If any concern: respond "rollback" and follow DEPLOY.md Rollback Procedure. The executor agent will NOT auto-deploy to VPS.
  </how-to-verify>
  <acceptance_criteria>
    - User explicitly confirms tests green, image size acceptable, /health responding correctly
    - User types "approved" to unblock VPS deploy (which happens outside this plan)
  </acceptance_criteria>
  <resume-signal>Type "approved" to mark Phase 07 complete, or "rollback" to trigger DEPLOY.md rollback procedure.</resume-signal>
</task>

</tasks>

<verification>
- Dockerfile contains `python:3.12-slim` twice, `python:3.11-slim` zero times
- Image builds successfully (`docker compose build nexus` exits 0)
- `docker compose run --rm nexus python -m pytest tests/ -q` exits 0
- `docker compose run --rm nexus python --version` shows 3.12.x
- Image size < 250MB
- requirements.lock.pre-python312.txt exists (rollback anchor)
- User has approved the checkpoint
</verification>

<success_criteria>
Python 3.12 upgrade committed, tests green, image under size budget, rollback anchor in place, human approved. Phase 07 (F6) complete.
</success_criteria>

<output>
Create `.planning/phases/07-f6-stack-modernization/07-03-SUMMARY.md` documenting: Dockerfile diff, final image size (numeric), pytest output inside container, `python --version` output, checkpoint outcome.

Also create `.planning/phases/07-f6-stack-modernization/07-PHASE-SUMMARY.md` aggregating Plans 01/02/03 outcomes and marking F6 complete in ROADMAP.md.
</output>
