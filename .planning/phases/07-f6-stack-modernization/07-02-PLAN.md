---
phase: 07-f6-stack-modernization
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - requirements.txt
  - modules/oathnet_client.py
autonomous: true
requirements:
  - F6-DEPCLEAN
  - FIND-16
must_haves:
  truths:
    - "requirements.txt does not contain tenacity"
    - "modules/oathnet_client.py has exactly one HTTP 429 check in _handle()"
    - "No code anywhere imports tenacity"
    - "Existing tests still pass after cleanup"
  artifacts:
    - path: "requirements.txt"
      provides: "Slim dependency list, tenacity removed"
    - path: "modules/oathnet_client.py"
      provides: "Deduplicated 429 handling (FIND-16 closed)"
  key_links:
    - from: "modules/oathnet_client.py _handle()"
      to: "single 429 branch"
      via: "if status == 429"
      pattern: "status == 429"
---

<objective>
Close FIND-16 (duplicate 429 handling in OathnetClient) and remove the unused `tenacity==8.5.0` dependency per D-03.

Purpose: Reduce attack surface and container size before the 3.12 upgrade (Plan 03). Clean dependency graph reduces risk that 3.12 compatibility check surfaces issues in packages we do not actually use.
Output: Smaller requirements.txt, deduplicated OathnetClient error handling.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/07-f6-stack-modernization/07-CONTEXT.md
@.planning/codebase/CONCERNS.md
@requirements.txt
@modules/oathnet_client.py
</context>

<tasks>

<task type="auto">
  <name>Task 1: Remove tenacity from requirements.txt (D-03)</name>
  <files>requirements.txt</files>
  <read_first>
    - requirements.txt (current content)
    - .planning/phases/07-f6-stack-modernization/07-CONTEXT.md §D-03
  </read_first>
  <action>
    1. Verify tenacity is truly unused. Run a repo-wide grep:
       ```bash
       grep -rn "tenacity\|from tenacity\|import tenacity" --include="*.py" .
       ```
       Expected: zero matches (or only matches inside venv/.venv/site-packages which should be excluded).
       If any application code imports tenacity, STOP and report — do not remove.

    2. Edit `requirements.txt` and delete the line `tenacity==8.5.0` (currently line 7 per file read). Preserve all other lines and comments exactly.

    3. Rebuild the local venv to ensure the lockfile is consistent:
       ```bash
       pip uninstall -y tenacity
       pip install -r requirements.txt
       ```

    4. Re-run the test suite to prove nothing silently depended on tenacity:
       ```bash
       python -m pytest tests/ -q
       ```
  </action>
  <verify>
    <automated>! grep -q "^tenacity" requirements.txt && ! grep -rn "^import tenacity\|^from tenacity" --include="*.py" api/ modules/ tests/ && python -m pytest tests/ -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep "tenacity" requirements.txt` returns no matches
    - `grep -rn "tenacity" --include="*.py" api/ modules/ tests/` returns no matches
    - `python -m pytest tests/ -q` exits 0 with the SAME test count as before removal
  </acceptance_criteria>
  <done>tenacity==8.5.0 removed, no imports reference it, full test suite still green.</done>
</task>

<task type="auto">
  <name>Task 2: Fix FIND-16 — verify and enforce single 429 branch in OathnetClient._handle()</name>
  <files>modules/oathnet_client.py</files>
  <read_first>
    - modules/oathnet_client.py lines 180-220 (current _handle() method)
    - .planning/codebase/CONCERNS.md §"Duplicate HTTP 429 Handling" (FIND-16 reference)
  </read_first>
  <action>
    CONTEXT: CONCERNS.md documents duplicate 429 checks at lines ~174 and ~179-180. A grep on the current file shows only ONE 429 branch at line 196. This suggests FIND-16 was partially addressed during the Phase 11 httpx migration. Your job is to CONFIRM and CERTIFY the fix, not to invent a new bug.

    Steps:

    1. Grep-audit the file:
       ```bash
       grep -n "429" modules/oathnet_client.py
       ```
       Expected: exactly ONE occurrence inside `_handle()` (the `if status == 429:` branch) and its error message. Additional mentions are only acceptable in docstrings/comments.

    2. If MORE than one 429 branch exists in `_handle()`: delete the duplicate (the block with the less-informative error message — keep "OathNet rate limit exceeded (HTTP 429). Wait before retrying.").

    3. If exactly one 429 branch exists (current state): add a brief comment above the branch to anchor the fix and prevent regression:
       ```python
       # FIND-16: single 429 check — do NOT duplicate. See .planning/codebase/CONCERNS.md
       if status == 429:
           return False, {"error": "OathNet rate limit exceeded (HTTP 429). Wait before retrying."}
       ```

    4. Verify the module still imports cleanly:
       ```bash
       python -c "from modules.oathnet_client import oathnet_client; print('ok')"
       ```

    5. Run OathNet unit tests:
       ```bash
       python -m pytest tests/test_oathnet_client.py -q
       ```
  </action>
  <verify>
    <automated>test $(grep -c "status == 429" modules/oathnet_client.py) -eq 1 && python -c "from modules.oathnet_client import oathnet_client; print('ok')" && python -m pytest tests/test_oathnet_client.py -q</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "status == 429" modules/oathnet_client.py` returns exactly `1`
    - `grep "FIND-16" modules/oathnet_client.py` returns at least 1 match (anchor comment)
    - `python -c "from modules.oathnet_client import oathnet_client"` exits 0
    - `python -m pytest tests/test_oathnet_client.py -q` exits 0
  </acceptance_criteria>
  <done>Exactly one 429 branch in _handle(), anchored by FIND-16 comment, oathnet tests green.</done>
</task>

</tasks>

<verification>
- `grep -c "^tenacity" requirements.txt` returns 0
- `grep -c "status == 429" modules/oathnet_client.py` returns 1
- `python -m pytest tests/ -q` exits 0
</verification>

<success_criteria>
Dependency cleanup (D-03) complete, FIND-16 certified closed. Ready for Plan 03 base-image upgrade.
</success_criteria>

<output>
Create `.planning/phases/07-f6-stack-modernization/07-02-SUMMARY.md` recording: tenacity removal confirmed, FIND-16 state before/after (single branch), test suite output.
</output>
