---
phase: 02-xss-sanitization
plan: 02
type: execute
wave: 1
depends_on: [02-01]
files_modified:
  - static/js/render.js
  - static/js/panels.js
  - static/js/history.js
autonomous: true
requirements:
  - XSS-03
  - XSS-04

must_haves:
  truths:
    - "All unescaped API string interpolations in render.js are wrapped with esc()"
    - "panels.js addModuleRow escapes SSE event label data before innerHTML insertion"
    - "history.js uses escAttr() for onclick attribute value context per D-09"
    - "grep across static/js/ files confirms no unescaped API string data in innerHTML"
  artifacts:
    - path: "static/js/render.js"
      provides: "esc(pwned) applied to victim date string"
      contains: "esc(pwned)"
    - path: "static/js/panels.js"
      provides: "esc(label) applied to SSE event label"
      contains: "esc(label)"
    - path: "static/js/history.js"
      provides: "escAttr(h.query) for onclick attribute context"
      contains: "escAttr(h.query)"
---

<objective>
Complete the escaping audit (XSS-03, XSS-04): wrap remaining unescaped API string interpolations with esc(), apply escAttr() for onclick attribute contexts, and verify via grep that zero unescaped API data reaches innerHTML across all JS files.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Fix remaining unescaped interpolations</name>
  <files>static/js/render.js, static/js/panels.js, static/js/history.js</files>
  <action>
- render.js line 785: `${pwned}` → `${esc(pwned)}` (date string from API, D-06)
- panels.js line 23: `${label}` → `${esc(label)}` (label from SSE event data)
- history.js line 30: onclick `esc(h.query)` → `escAttr(h.query)` (attribute value context, D-09)
  </action>
  <verify>
    <automated>grep -n "esc(pwned)" static/js/render.js && grep -n "esc(label)" static/js/panels.js && grep -n "escAttr" static/js/history.js</automated>
  </verify>
</task>
</tasks>
