---
phase: 19-postgres-container-compose-wiring-parallel-deploy
plan: 02
type: execute
wave: 2
depends_on: ["19-01"]
files_modified:
  - DEPLOY.md
  - .planning/phases/19-postgres-container-compose-wiring-parallel-deploy/19-02-SUMMARY.md
autonomous: false
requirements: [DBM-05, DBM-06, DBM-07, DBM-08, DBM-09, DBM-10]
requirements_addressed: [DBM-05, DBM-06, DBM-07, DBM-08, DBM-09, DBM-10]

must_haves:
  truths:
    - "docker compose config succeeds after Postgres wiring"
    - "Postgres starts healthy locally or on VPS with Docker secret present"
    - "Nexus remains healthy and still on SQLite"
    - "Postgres exposes no public 5432 port"
    - "VPS deploy creates the pg_password secret file before compose up"
  artifacts:
    - path: "DEPLOY.md"
      provides: "Postgres secret/deploy operator note"
      contains: "secrets/pg_password.txt"
    - path: ".planning/phases/19-postgres-container-compose-wiring-parallel-deploy/19-02-SUMMARY.md"
      provides: "Runtime smoke evidence"
      contains: "pg_isready"
  key_links:
    - from: "DEPLOY.md"
      to: "docker-compose.yml"
      via: "secret file requirement"
      pattern: "secrets/pg_password.txt"
---

<objective>
Document the Postgres secret/deploy requirement, run Compose validation, deploy the parallel Postgres service to Hetzner, and capture runtime smoke evidence.

Purpose: prove Phase 19 infrastructure is not merely configured but healthy in the production Compose stack.
Output: deploy note plus SUMMARY evidence showing healthy Postgres, healthy Nexus, no public Postgres port, and memory within budget.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@CLAUDE.md
@DEPLOY.md
@docker-compose.yml
@.planning/phases/19-postgres-container-compose-wiring-parallel-deploy/19-CONTEXT.md
@.planning/phases/19-postgres-container-compose-wiring-parallel-deploy/19-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Document secret bootstrap requirement</name>
  <files>DEPLOY.md</files>
  <read_first>DEPLOY.md, docker-compose.yml, .planning/phases/19-postgres-container-compose-wiring-parallel-deploy/19-CONTEXT.md</read_first>
  <action>
    Update `DEPLOY.md` with a short Phase 19 Postgres note:
    - Secret file path: `secrets/pg_password.txt`.
    - Secret file must exist on VPS before `docker compose up -d --build`.
    - Command example:
      `mkdir -p secrets && chmod 700 secrets`
      `printf '%s' 'REPLACE_WITH_STRONG_PASSWORD' > secrets/pg_password.txt`
      `chmod 600 secrets/pg_password.txt`
    - State explicitly: never commit the secret file.
    - State explicitly: Phase 19 does not flip Nexus to Postgres; app remains SQLite until Phase 24 cutover.
  </action>
  <verify>rg -n "secrets/pg_password.txt|Phase 19|SQLite|Postgres" DEPLOY.md</verify>
  <acceptance_criteria>
    - `DEPLOY.md` contains `secrets/pg_password.txt`.
    - `DEPLOY.md` contains `never commit`.
    - `DEPLOY.md` states app remains SQLite until cutover.
  </acceptance_criteria>
  <done>Operator runbook prevents missing-secret startup failure.</done>
</task>

<task type="manual">
  <name>Task 2: Run local Compose validation</name>
  <files>docker-compose.yml</files>
  <read_first>docker-compose.yml</read_first>
  <action>
    Run local static validation:
    - `docker compose config`
    - `rg -n "postgres:16-alpine|POSTGRES_PASSWORD_FILE|pg_password|postgres_data|condition: service_healthy|memory: 2500m|mem_limit: 768m|shm_size: 256mb|name: nexus_net" docker-compose.yml`
    - Verify manually that the `postgres` service has no `ports:` key.

    If Docker is unavailable locally, record that and continue to VPS validation after commit.
  </action>
  <verify>docker compose config</verify>
  <acceptance_criteria>
    - `docker compose config` exits 0, or SUMMARY records local Docker unavailable with exact error.
    - Grep finds all locked Compose strings.
    - SUMMARY states whether Postgres has any public port mapping.
  </acceptance_criteria>
  <done>Compose syntax and static DBM coverage validated.</done>
</task>

<task type="manual">
  <name>Task 3: Deploy parallel Postgres to Hetzner and smoke-test</name>
  <files>.planning/phases/19-postgres-container-compose-wiring-parallel-deploy/19-02-SUMMARY.md</files>
  <read_first>CLAUDE.md, DEPLOY.md, docker-compose.yml</read_first>
  <action>
    After Plan 19-01 and Task 1 are committed:
    - Push `master`.
    - On VPS `hetzner`, ensure `/home/deploy/nexus-osint/secrets/pg_password.txt` exists with mode `600`.
    - Deploy the committed tree by the existing project deploy method.
    - Run `docker compose up -d --build`.
    - Run `docker compose ps`.
    - Run `docker exec nexus-postgres pg_isready -U nexus -d nexusosint`.
    - Run public health check `https://nexusosint.uk/health`.
    - Run `docker stats --no-stream nexus-postgres nexus-osint`.
    - Verify `docker compose ps` shows no `0.0.0.0:5432` or `[::]:5432` mapping.
    - Capture key outputs in `19-02-SUMMARY.md`.

    Do not run migrations. Do not set `DATABASE_URL`. Do not modify `.env` beyond creating the secret file.
  </action>
  <verify>ssh hetzner "cd ~/nexus-osint && docker compose ps && docker exec nexus-postgres pg_isready -U nexus -d nexusosint"</verify>
  <acceptance_criteria>
    - `docker exec nexus-postgres pg_isready -U nexus -d nexusosint` exits 0.
    - `docker compose ps` shows `nexus-postgres` healthy.
    - Public `/health` returns `status: healthy`.
    - `docker compose ps` does not show public Postgres port `5432`.
    - `docker stats --no-stream` shows Postgres below `768MiB` and Nexus below `2500MiB` at rest, or SUMMARY records exact values and flags if exceeded.
    - SUMMARY confirms app still on SQLite because `DATABASE_URL` is absent.
  </acceptance_criteria>
  <done>Production has parallel healthy Postgres with no cutover.</done>
</task>

</tasks>

<verification>
- `docker compose config`
- `docker exec nexus-postgres pg_isready -U nexus -d nexusosint`
- `curl -fsS https://nexusosint.uk/health`
- `docker stats --no-stream nexus-postgres nexus-osint`
</verification>

<success_criteria>
- DBM-05 through DBM-10 are runtime-verified, not only statically configured.
- VPS deploy is complete.
- App remains on SQLite.
- Summary file `.planning/phases/19-postgres-container-compose-wiring-parallel-deploy/19-02-SUMMARY.md` contains evidence.
</success_criteria>

<output>
After completion, create `.planning/phases/19-postgres-container-compose-wiring-parallel-deploy/19-02-SUMMARY.md` with local config validation, VPS smoke results, memory stats, public port check, and deployment notes.
</output>
