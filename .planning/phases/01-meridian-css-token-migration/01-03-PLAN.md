---
phase: 01-meridian-css-token-migration
plan: 03
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - static/css/tables.css
autonomous: true
requirements: [CSS-01, CSS-02, CSS-03, CSS-06, CSS-07, CSS-08]

must_haves:
  truths:
    - "tables.css has zero legacy tokens remaining"
    - "All hardcoded rgba() color values in tables.css are replaced by named tokens"
    - "Severity row badge borders use token-based colors"
    - "Social badge colors use --color-success-* tokens"
  artifacts:
    - path: "static/css/tables.css"
      provides: "Data table, severity row, social badge, pagination, password masking styles using Meridian tokens"
      contains: "--color-text-tertiary"
  key_links:
    - from: "static/css/tables.css"
      to: "static/css/tokens.css"
      via: "var(--color-*) and var(--radius-*)"
      pattern: "var\\(--color-"
---

<objective>
Migrate tables.css from legacy tokens to Meridian tokens. This 104-line file handles data tables, severity rows, social badges, pagination, password masking, and the load-more button. It has 30 legacy token occurrences.

Purpose: Eliminate all --var, --amber, --bg, --line, --green, --red, --orange, --mono, --dur-fast references and replace with Meridian equivalents. Fix the social badge border-radius (5px → --radius-md at 4px).

Output: tables.css using only Meridian semantic tokens.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@BRIEFING_IMPLEMENTACAO.md
@.planning/phases/01-meridian-css-token-migration/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Token mapping (per BRIEFING §3.3 and plan 01) — same reference as plan 02 -->
OLD TOKEN → NEW TOKEN
--text    → --color-text-primary
--text2   → --color-text-secondary
--text3   → --color-text-tertiary
--amber   → --color-accent
--amber-lo → --color-accent-muted
--amber-glow → --color-accent-glow
--red     → --color-critical
--red-lo  → --color-critical-muted
--orange  → --color-high
--green   → --color-success
--green-lo → --color-success-muted
--blue    → --color-info
--mono    → --font-data
--bg4     → --color-surface-2
--bg3     → --color-surface-1
--bg5     → --color-surface-3
--line    → --color-border-subtle
--line2   → --color-border-default
--dur-fast → --duration-fast

RGBA → TOKEN (severity/social context)
rgba(232,64,64,.04)   → --color-critical-muted  (severity-critical row bg — but .04 opacity lighter than token .10, keep as literal)
rgba(232,130,42,.03)  → var(--color-high-muted) — but .03 is very light, keep literal
rgba(232,64,64,.2)    → var(--color-critical-border)  → actually use --color-critical-muted for the badge
rgba(232,130,42,.2)   → var(--color-high-muted) for badge border
rgba(245,166,35,.2)   → var(--color-accent-border) for badge border
rgba(62,199,140,.2)   → var(--color-success-border) for social badge border
rgba(62,199,140,.18)  → var(--color-success-muted) for social badge hover bg
rgba(62,199,140,.18)  → var(--color-success-muted) (same)
rgba(245,166,35,.04)  → var(--color-accent-muted) for load-more hover
rgba(245,166,35,.35)  → var(--color-accent-border) for load-more hover border
rgba(255,255,255,.025) → var(--color-border-subtle) for table row hover

BORDER-RADIUS RULES (tables.css):
.sev-breach-badge: 3px → var(--radius-sm)
.social-badge: 5px → var(--radius-md) (4px)
.pwd-toggle: 3px → var(--radius-sm)
.load-more-btn: 6px → var(--radius-lg)
.pagination and other elements: keep any existing token references

SHADOW RULES:
0 4px 12px rgba(62,199,140,.18) → 0 4px 12px var(--color-success-muted) [social badge hover shadow — keep directional, replace rgba]
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate tables.css</name>
  <files>static/css/tables.css</files>

  <read_first>
    - static/css/tables.css — read the CURRENT file in full. It is 105 lines. Map every legacy token and rgba value before making changes. The file contains: data-table, severity rows (sev-critical/high/medium/low), breach badges, social-grid, social-badge, pagination, pwd-cell/pwd-text/pwd-toggle, and load-more-btn.
    - BRIEFING_IMPLEMENTACAO.md §3.3 for the token mapping table.
  </read_first>

  <action>
    Rewrite static/css/tables.css making exactly these replacements. Do NOT change structural CSS (padding values, font-size, layout properties).

    LEGACY TOKEN REPLACEMENTS (line-by-line):
    1. .data-table font-family: var(--mono) → var(--font-data)
    2. .data-table th color: var(--text3) → var(--color-text-tertiary)
    3. .data-table th border-bottom: var(--line) → var(--color-border-subtle)
    4. .data-table td border-bottom: var(--line) → var(--color-border-subtle)
    5. .data-table td color: var(--text2) → var(--color-text-secondary)
    6. .data-table tr transition: ...var(--dur-fast) var(--ease-in-out) → var(--duration-fast) var(--ease-in-out)
    7. .data-table .val-danger color: var(--red) → var(--color-critical)
    8. .data-table .val-warn color: var(--orange) → var(--color-high)
    9. .data-table .val-safe color: var(--green) → var(--color-success)
    10. .data-table .val-amber color: var(--amber) → var(--color-accent)
    11. .data-table .val-muted color: var(--text3) → var(--color-text-tertiary)
    12. .sev-critical td:first-child border-left: var(--red) → var(--color-critical)
    13. .sev-high td:first-child border-left: var(--orange) → var(--color-high)
    14. .sev-medium td:first-child border-left: var(--amber) → var(--color-accent)
    15. .sev-critical .sev-breach-badge: background var(--red-lo) → var(--color-critical-muted); color var(--red) → var(--color-critical)
    16. .sev-high .sev-breach-badge: color var(--orange) → var(--color-high)
    17. .sev-medium .sev-breach-badge: background var(--amber-lo) → var(--color-accent-muted); color var(--amber) → var(--color-accent)
    18. .sev-low .sev-breach-badge: background var(--bg4) → var(--color-surface-2); color var(--text3) → var(--color-text-tertiary); border var(--line) → var(--color-border-subtle)
    19. .social-badge background: var(--green-lo) → var(--color-success-muted); color var(--green) → var(--color-success)
    20. .pagination color: var(--text2) → var(--color-text-secondary); font-family var(--mono) → var(--font-data)
    21. .pwd-text.masked color: var(--text3) → var(--color-text-tertiary)
    22. .pwd-toggle border: var(--line) → var(--color-border-subtle); color var(--text3) → var(--color-text-tertiary)
    23. .pwd-toggle transition: ...var(--dur-fast) var(--ease-in-out) → var(--duration-fast) var(--ease-in-out)
    24. .pwd-toggle:hover color: var(--amber) → var(--color-accent); border-color var(--amber) → var(--color-accent); background var(--amber-lo) → var(--color-accent-muted)
    25. .load-more-btn background: var(--bg3) → var(--color-surface-1); border-color var(--line2) → var(--color-border-default); color var(--text3) → var(--color-text-tertiary); font-family var(--mono) → var(--font-data)
    26. .load-more-btn transition: ...var(--dur-fast) var(--ease-out) → var(--duration-fast) var(--ease-out)
    27. .load-more-btn:hover color: var(--amber) → var(--color-accent)

    HARDCODED RGBA REPLACEMENTS:
    - .sev-critical .sev-breach-badge border: rgba(232,64,64,.2) → var(--color-critical-muted)
    - .sev-high .sev-breach-badge: background rgba(232,130,42,.1) → var(--color-high-muted); border rgba(232,130,42,.2) → var(--color-high-muted)
    - .sev-medium .sev-breach-badge border: rgba(245,166,35,.2) → var(--color-accent-muted)
    - .social-badge border: rgba(62,199,140,.2) → var(--color-success-muted)
    - .social-badge:hover box-shadow: 0 4px 12px rgba(62,199,140,.18) → 0 4px 12px var(--color-success-muted)
    - .social-badge:hover background: rgba(62,199,140,.18) → var(--color-success-muted)
    - .data-table tr:hover td background: rgba(255,255,255,.025) → var(--color-border-subtle)
    - .load-more-btn:hover background: rgba(245,166,35,.04) → var(--color-accent-muted)
    - .load-more-btn:hover border-color: rgba(245,166,35,.35) → var(--color-accent-border)
    - .sev-critical td background: rgba(232,64,64,.04) — this is .04 opacity, lighter than --color-critical-muted (.10). Keep as literal for visual fidelity. Note: this is a table row background tint, not a border. Keep as rgba(239, 68, 68, 0.04) [updated to new red value].
    - .sev-high td background: rgba(232,130,42,.03) → rgba(249, 115, 22, 0.03) [update to new orange value, keep very low opacity as literal]

    BORDER-RADIUS REPLACEMENTS:
    - .sev-breach-badge border-radius: 3px → var(--radius-sm)
    - .social-badge border-radius: 5px → var(--radius-md)
    - .pwd-toggle border-radius: 3px → var(--radius-sm)
    - .load-more-btn border-radius: 6px → var(--radius-lg)
  </action>

  <verify>
    <automated>grep -E "var\(--text\b|var\(--text2\b|var\(--text3\b|var\(--amber\b|var\(--amber-lo\b|var\(--red\b|var\(--red-lo\b|var\(--orange\b|var\(--green\b|var\(--green-lo\b|var\(--mono\b|var\(--bg3\b|var\(--bg4\b|var\(--line\b|var\(--line2\b|var\(--dur-" static/css/tables.css</automated>
    Expect: no output

    <automated>grep "color-critical" static/css/tables.css</automated>
    Expect: multiple lines (severity critical rows and badges)

    <automated>grep "color-success" static/css/tables.css</automated>
    Expect: social badge lines
  </verify>

  <acceptance_criteria>
    - grep -E "var\(--text\b|var\(--text2\b|var\(--text3\b|var\(--amber\b|var\(--red\b|var\(--orange\b|var\(--green\b|var\(--mono\b|var\(--bg[0-9]\b|var\(--line\b|var\(--line2\b|var\(--dur-" static/css/tables.css returns no output
    - grep "color-critical" static/css/tables.css returns at least 3 matches (sev row, badge bg, badge color, border-left)
    - grep "color-success" static/css/tables.css returns at least 2 matches (social-badge bg and color)
    - grep "color-accent" static/css/tables.css returns at least 2 matches (amber column value, medium severity)
    - grep "radius-sm" static/css/tables.css returns 2 matches (sev-breach-badge and pwd-toggle)
    - grep "radius-md" static/css/tables.css returns 1 match (social-badge)
    - grep "radius-lg" static/css/tables.css returns 1 match (load-more-btn)
    - grep "duration-fast" static/css/tables.css returns at least 2 matches
    - grep "font-data" static/css/tables.css returns at least 2 matches
  </acceptance_criteria>

  <done>tables.css fully migrated to Meridian tokens. All legacy --var, --amber, --bg, --line, --green, --red, --orange, --mono, --dur-fast replaced. Border-radius values use radius tokens. Visual output identical.</done>
</task>

</tasks>

<verification>
After completion:
- grep -E "var\(--[a-z]+\b" static/css/tables.css | grep -v "color-\|font-\|space-\|radius-\|shadow-\|duration-\|ease-\|z-\|weight-\|text-" returns no output
  (confirms no legacy tokens remain)
</verification>

<success_criteria>
tables.css fully migrated to Meridian tokens. Zero occurrences of legacy token names (--text, --amber, --red, --orange, --green, --mono, --bg*, --line*, --dur-*). All severity colors use --color-critical/high/medium/low families. Social badge uses --color-success family. Table row hover uses --color-border-subtle. Load-more button uses accent tokens.
</success_criteria>

<output>
After completion, create .planning/phases/01-meridian-css-token-migration/01-03-SUMMARY.md with:
- Count of legacy token replacements made
- List of rgba() values kept as literals with justification (the .03/.04 opacity severity row backgrounds)
- Confirmation that border-radius values now use radius tokens
</output>
