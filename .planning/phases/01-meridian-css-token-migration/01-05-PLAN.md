---
phase: 01-meridian-css-token-migration
plan: 05
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - static/css/panels.css
autonomous: true
requirements: [CSS-01, CSS-02, CSS-03, CSS-04, CSS-05, CSS-07, CSS-08, CSS-09]

must_haves:
  truths:
    - "panels.css has zero legacy tokens remaining"
    - "All border-radius values > 6px are corrected: 10px, 11px, 12px all become var(--radius-lg)"
    - "All rgba(245,166,35,...) accent values use --color-accent-* tokens"
    - "Z-index values use --z-* tokens where applicable"
    - "All transition durations use --duration-* tokens"
  artifacts:
    - path: "static/css/panels.css"
      provides: "Quota, scan status, results, stat cards, and panel styles using Meridian tokens"
      contains: "--color-accent"
  key_links:
    - from: "static/css/panels.css"
      to: "static/css/tokens.css"
      via: "var(--color-*), var(--radius-*), var(--duration-*)"
      pattern: "var\\(--color-"
---

<objective>
Migrate panels.css from legacy tokens to Meridian tokens. This is a 291-line file with 61 legacy token occurrences — the second highest count. It covers quota bars, quota pills, scan status, result headers, stat cards, and collapsible result panels.

Purpose: Fix border-radius violations (12px appears at lines 43, 119, 194, and 11px at panel header corners). All --dur-fast/mid legacy durations must become --duration-fast/mid. The scan fill gradient (rgba(245,166,35,.7)) and modulePulse keyframe rgba must be token-referenced.

Output: panels.css using only Meridian tokens, all border-radius corrections applied.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@BRIEFING_IMPLEMENTACAO.md
@.planning/phases/01-meridian-css-token-migration/01-01-SUMMARY.md
</context>

<interfaces>
<!-- Same token mapping as plans 02-04. Key entries for panels.css: -->
OLD → NEW
--bg2  → --color-bg-recessed
--bg3  → --color-surface-1
--bg4  → --color-surface-2
--bg5  → --color-surface-3
--line  → --color-border-subtle
--line2 → --color-border-default
--amber → --color-accent
--amber-lo → --color-accent-muted (not directly used but rgba alternatives map here)
--amber-glow → --color-accent-glow
--red   → --color-critical
--green → --color-success
--orange → --color-high
--text  → --color-text-primary
--text2 → --color-text-secondary
--text3 → --color-text-tertiary
--mono  → --font-data
--sans  → --font-display (not used in panels.css)
--dur-fast → --duration-fast
--dur-mid  → --duration-mid

RGBA → TOKEN (panels.css context)
rgba(17,20,30,.85)     → var(--color-surface-2) [quota-pill background — dark surface]
rgba(255,255,255,.07)  → var(--color-border-subtle)
rgba(255,255,255,.08)  → var(--color-border-subtle)
rgba(255,255,255,.09)  → var(--color-border-default)
rgba(255,255,255,.06)  → var(--color-border-subtle)
rgba(11,13,20,.88)     → var(--color-surface-glass)  [scan-status background]
rgba(245,166,35,.1)    → var(--color-accent-muted)  [scan-status border]
rgba(245,166,35,.7)    → var(--color-accent-glow)  [scan fill gradient end — it's a faded amber. Use: linear-gradient(90deg, var(--color-accent), var(--color-accent-glow))]
rgba(245,166,35,.3)    → var(--color-accent-muted)  [modulePulse keyframe]
rgba(11,13,20,.92)     → var(--color-surface-glass)  [result-header background]
rgba(255,255,255,.06) in panel:hover border-color → var(--color-border-subtle)
rgba(23,27,40,.88) in .panel-header:hover → var(--color-surface-2) [close to surface-2 value]

BORDER-RADIUS VIOLATIONS (CSS-03):
.quota-bar: var(--r) → var(--radius-lg)  [--r was 8px, now 6px]
.quota-pill: 8px → var(--radius-lg)   [8px → 6px]
.quota-track: 3px → keep as 3px literal [track bar, too small for a named token]
.scan-status: 12px → var(--radius-lg)   [12px → 6px]
.result-header: 12px → var(--radius-lg) [12px → 6px]
.risk-badge: 8px → var(--radius-lg)     [8px → 6px]
.stat-card: 10px → var(--radius-lg)     [10px → 6px]
.panel: 12px → var(--radius-lg)         [12px → 6px]
.panel-header: 11px 11px 0 0 → 6px 6px 0 0 (use var(--radius-lg) in shorthand: var(--radius-lg) var(--radius-lg) 0 0)
.panel.open .panel-header border-radius: 11px 11px 0 0 → var(--radius-lg) var(--radius-lg) 0 0
.panel-not-run-badge: 3px → var(--radius-sm)

NOTE on .quota-track, .quota-fill, .quota-pill-track, .quota-pill-fill border-radius: 3px — these are track/fill bars (tiny functional elements, not card borders). Keep as 3px literal — they are not border-radius-of-a-card, they are cylindrical bar ends.

Z-INDEX: panels.css does not appear to have hardcoded z-index values. Skip.

KEYFRAME RGBA:
@keyframes modulePulse:
  0%,100%: box-shadow: 0 0 4px var(--amber) → 0 0 4px var(--color-accent)
  50%: box-shadow: 0 0 10px var(--amber), 0 0 20px rgba(245,166,35,.3) → 0 0 10px var(--color-accent), 0 0 20px var(--color-accent-muted)
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Migrate panels.css</name>
  <files>static/css/panels.css</files>

  <read_first>
    - static/css/panels.css — read the CURRENT file in full. It is 291 lines. The file is structured: quota-bar (lines 6-21), quota-pill (lines 23-36), scan-status (lines 39-107), results (lines 109-138), stat-grid/stat-card (lines 151-188), panel (lines 190-258), keyframes (lines 269-290).
    - Note the border-radius values that violate the design system: line 9 (var(--r)), line 29 (8px), line 43 (12px), line 119 (12px), line 147 (8px), line 158 (10px), line 194 (12px), line 209 (11px 11px 0 0), line 215 (11px 11px 0 0).
    - BRIEFING_IMPLEMENTACAO.md §3.3 for the complete mapping.
  </read_first>

  <action>
    Rewrite static/css/panels.css making exactly these replacements. Do NOT change structural CSS (padding, margin, font-size, width, height values unless explicitly stated).

    SECTION: .quota-bar
    - background: var(--bg3) → var(--color-surface-1)
    - border: var(--line) → var(--color-border-subtle)
    - border-radius: var(--r) → var(--radius-lg)
    - font-family: var(--mono) → var(--font-data)
    - color: var(--text2) → var(--color-text-secondary)
    - .quota-track background: var(--bg5) → var(--color-surface-3)
    - .quota-fill background: var(--green) → var(--color-success)
    - .quota-fill.warn background: var(--orange) → var(--color-high)
    - .quota-fill.crit background: var(--red) → var(--color-critical)
    - .quota-text color: var(--amber) → var(--color-accent)

    SECTION: .quota-pill
    - background: rgba(17,20,30,.85) → var(--color-surface-2)
    - border: rgba(255,255,255,.07) → var(--color-border-subtle)
    - border-radius: 8px → var(--radius-lg)
    - font-family: var(--mono) → var(--font-data)
    - color: var(--text2) → var(--color-text-secondary)
    - .quota-pill-track background: var(--bg5) → var(--color-surface-3)
    - .quota-pill-fill background: var(--green) → var(--color-success)
    - .quota-pill-fill.warn background: var(--orange) → var(--color-high)
    - .quota-pill-fill.crit background: var(--red) → var(--color-critical)
    Note: .quota-pill-track and .quota-pill-fill border-radius: 3px — keep as literal 3px (track bars)

    SECTION: .scan-status
    - background: rgba(11,13,20,.88) → var(--color-surface-glass)
    - border: rgba(245,166,35,.1) → var(--color-accent-muted)
    - border-radius: 12px → var(--radius-lg)
    - .scan-title font-family: var(--mono) → var(--font-data)
    - .scan-title color: var(--text) → var(--color-text-primary)
    - .scan-title::after color: var(--amber) → var(--color-accent)
    - .scan-pct font-family: var(--mono) → var(--font-data)
    - .scan-pct color: var(--amber) → var(--color-accent)
    - .scan-track background: var(--bg5) → var(--color-surface-3)
    - .scan-fill gradient: linear-gradient(90deg, var(--amber), rgba(245,166,35,.7)) → linear-gradient(90deg, var(--color-accent), var(--color-accent-glow))
    - .scan-fill::after background: var(--amber) → var(--color-accent)
    - .scan-fill::after box-shadow: 0 0 8px var(--amber) → 0 0 8px var(--color-accent)
    - .scan-module font-family: var(--mono) → var(--font-data)
    - .scan-module color: var(--text3) → var(--color-text-tertiary)
    - .scan-module transition: var(--dur-fast) → var(--duration-fast)
    - .scan-module.active color: var(--amber) → var(--color-accent)
    - .scan-module.done color: var(--green) → var(--color-success)
    - .scan-module.error color: var(--red) → var(--color-critical)
    - .scan-module.error .scan-module-dot background and box-shadow: var(--red) → var(--color-critical)
    - .scan-module-dot background: var(--text3) → var(--color-text-tertiary)
    - .scan-module-dot transition: var(--dur-fast) → var(--duration-fast)
    - .scan-module.active .scan-module-dot background: var(--amber) → var(--color-accent)
    - .scan-module.active .scan-module-dot box-shadow: 0 0 6px var(--amber) → 0 0 6px var(--color-accent)
    - .scan-module.done .scan-module-dot background: var(--green) → var(--color-success)

    SECTION: .result-header
    - background: rgba(11,13,20,.92) → var(--color-surface-glass)
    - border: rgba(255,255,255,.08) → var(--color-border-subtle)
    - border-radius: 12px → var(--radius-lg)
    - .result-status color: var(--green) → var(--color-success)
    - .result-status::before background: var(--green) → var(--color-success)
    - .result-status::before box-shadow: 0 0 6px var(--green) → 0 0 6px var(--color-success)
    - .result-target color: var(--text) → var(--color-text-primary)
    - .result-target font-family: var(--mono) → var(--font-data)
    - .result-sub color: var(--text2) → var(--color-text-secondary)
    - .result-sub font-family: var(--mono) → var(--font-data)

    SECTION: .risk-badge
    - border-radius: 8px → var(--radius-lg)

    SECTION: .stat-card
    - background: var(--bg2) → var(--color-bg-recessed)
    - border: var(--line) → var(--color-border-subtle)
    - border-radius: 10px → var(--radius-lg)
    - transition: var(--dur-mid) → var(--duration-mid)
    - .stat-card:hover border-color: var(--line2) → var(--color-border-default)
    - .stat-card:hover background: var(--bg3) → var(--color-surface-1)
    - .stat-card-val font-family: var(--mono) → var(--font-data)
    - .stat-card-val color: var(--text) → var(--color-text-primary)
    - .stat-card-lbl color: var(--text3) → var(--color-text-tertiary)
    - .stat-card-note font-family: var(--mono) → var(--font-data)

    SECTION: .panel
    - background: var(--bg2) → var(--color-bg-recessed)
    - border: var(--line) → var(--color-border-subtle)
    - border-radius: 12px → var(--radius-lg)
    - .panel transition: var(--dur-fast) → var(--duration-fast)
    - .panel:hover border-color: rgba(255,255,255,.09) → var(--color-border-default)
    - .panel-header background: var(--bg3) → var(--color-surface-1)
    - .panel-header border-radius: 11px 11px 0 0 → var(--radius-lg) var(--radius-lg) 0 0
    - .panel-header transition: var(--dur-fast) → var(--duration-fast)
    - .panel-header:hover background: rgba(23,27,40,.88) → var(--color-surface-2)
    - .panel.open .panel-header border-bottom-color: rgba(255,255,255,.06) → var(--color-border-subtle)
    - .panel.open .panel-header border-radius: 11px 11px 0 0 → var(--radius-lg) var(--radius-lg) 0 0
    - .panel-title color: var(--text) → var(--color-text-primary)
    - .panel-badge font-family: var(--mono) → var(--font-data)
    - .panel-badge background: var(--bg5) → var(--color-surface-3)
    - .panel-badge border: var(--line2) → var(--color-border-default)
    - .panel-badge color: var(--text2) → var(--color-text-secondary)
    - .panel-chevron color: var(--text3) → var(--color-text-tertiary)
    - .panel-body padding kept as-is
    - .panel.open .panel-body animation: var(--dur-fast) → var(--duration-fast)
    - .panel-not-run-badge font-family: var(--mono) → var(--font-data)
    - .panel-not-run-badge color: var(--text3) → var(--color-text-tertiary)
    - .panel-not-run-badge background: var(--bg5) → var(--color-surface-3)
    - .panel-not-run-badge border: var(--line) → var(--color-border-subtle)
    - .panel-not-run-badge border-radius: 3px → var(--radius-sm)

    SECTION: keyframes
    - @keyframes modulePulse 0%,100%: 0 0 4px var(--amber) → 0 0 4px var(--color-accent)
    - @keyframes modulePulse 50%: 0 0 10px var(--amber) → 0 0 10px var(--color-accent); rgba(245,166,35,.3) → var(--color-accent-muted)
  </action>

  <verify>
    <automated>grep -E "var\(--bg[^-]|var\(--bg2\b|var\(--bg3\b|var\(--bg5\b|var\(--text\b|var\(--text2\b|var\(--text3\b|var\(--amber\b|var\(--red\b|var\(--green\b|var\(--orange\b|var\(--mono\b|var\(--line\b|var\(--line2\b|var\(--dur-|var\(--r\b" static/css/panels.css</automated>
    Expect: no output

    <automated>grep "border-radius.*12px\|border-radius.*11px\|border-radius.*10px\|border-radius.*8px" static/css/panels.css</automated>
    Expect: no output (all over-6px border-radius values replaced)

    <automated>grep "radius-lg" static/css/panels.css</automated>
    Expect: multiple matches (quota-bar, quota-pill, scan-status, result-header, risk-badge, stat-card, panel, panel-header)
  </verify>

  <acceptance_criteria>
    - grep -E "var\(--bg[0-9]\b|var\(--text\b|var\(--amber\b|var\(--red\b|var\(--green\b|var\(--orange\b|var\(--mono\b|var\(--line\b|var\(--line2\b|var\(--dur-|var\(--r\b" static/css/panels.css returns no output
    - grep "border-radius.*12px\|border-radius.*11px\|border-radius.*10px\|border-radius.*8px" static/css/panels.css returns no output
    - grep "radius-lg" static/css/panels.css returns at least 7 matches
    - grep "radius-sm" static/css/panels.css returns the panel-not-run-badge line
    - grep "color-accent" static/css/panels.css returns at least 8 matches
    - grep "color-success" static/css/panels.css returns at least 4 matches (result-status, quota fills)
    - grep "duration-fast" static/css/panels.css returns at least 5 matches
  </acceptance_criteria>

  <done>panels.css fully migrated to Meridian tokens. All 61 legacy token occurrences replaced. Border-radius violations (12px, 11px, 10px, 8px) corrected to var(--radius-lg). Scan fill gradient uses color-accent tokens. Keyframe rgba values use token references.</done>
</task>

</tasks>

<verification>
After completion:
- grep -E "var\(--[a-z][a-z0-9]*\b" static/css/panels.css | grep -vE "color-|font-|space-|radius-|shadow-|duration-|ease-|z-|weight-|text-" returns no output
- grep -E "border-radius: [0-9]+(px)" static/css/panels.css | grep -vE "border-radius: [234]px|border-radius: 999px" returns no lines with values > 6px
</verification>

<success_criteria>
panels.css fully migrated. Zero legacy tokens remain. All border-radius values > 6px corrected to var(--radius-lg) or appropriate radius token. Scan animation uses color-accent tokens. Result header, stat cards, and panels use Meridian surface/border tokens.
</success_criteria>

<output>
After completion, create .planning/phases/01-meridian-css-token-migration/01-05-SUMMARY.md with:
- List of border-radius corrections made (line and old → new value)
- Count of legacy token replacements
- Confirmation keyframe rgba values updated
</output>
