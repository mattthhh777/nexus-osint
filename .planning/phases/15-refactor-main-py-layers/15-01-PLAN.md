---
phase: 15-refactor-main-py-layers
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - api/schemas.py
  - api/main.py
  - tests/test_endpoints.py
  - tests/unit/test_security_gates.py
  - tests/integration/test_rate_limiting.py
autonomous: true
risk: LOW
requirements: [REFACTOR-15-STEP-1]

must_haves:
  truths:
    - "Pydantic I/O models live in api/schemas.py and are imported from there by api/main.py"
    - "pytest -q still reports 62 tests passed, exit code 0"
    - "docker compose build succeeds with zero errors"
    - "docker compose up brings the app healthy in < 15s and curl /health returns 200"
    - "Smoke: login with admin + one search request completes end-to-end"
    - "grep 'from api.main import' across the repo is monotonically non-increasing vs baseline"
  artifacts:
    - path: "api/schemas.py"
      provides: "All Pydantic request/response models: LoginRequest, SearchRequest"
      contains: "class LoginRequest"
    - path: "api/main.py"
      provides: "Monolith with Pydantic classes removed and re-imported from api.schemas"
      contains: "from api.schemas import"
  key_links:
    - from: "api/main.py"
      to: "api/schemas.py"
      via: "from api.schemas import LoginRequest, SearchRequest"
      pattern: "from api\\.schemas import"
    - from: "api/main.py endpoints (login, search, more_breaches)"
      to: "api.schemas.LoginRequest, api.schemas.SearchRequest"
      via: "FastAPI body parameter typing"
      pattern: "body: LoginRequest|req: SearchRequest"
---

<objective>
Extract all Pydantic I/O models from `api/main.py` into a dedicated `api/schemas.py` module. This is Step 1 of the horizontal-slice refactor locked in D-03 (CONTEXT.md). Risk is LOW: schemas are pure data classes with zero runtime state.

Purpose: establish the leaf module of the dependency tree before services/routes extraction. `api/schemas.py` is forbidden from importing any other internal module (per import contract in CONTEXT.md) — validators live with their model.

Output: new `api/schemas.py` holding `LoginRequest` and `SearchRequest` (with all field validators preserved verbatim). `api/main.py` re-imports them so every call site continues to work. Test imports adapted where needed. Suite stays green (62/62).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/15-refactor-main-py-layers/15-CONTEXT.md
@.planning/codebase/STRUCTURE.md
@.planning/codebase/ARCHITECTURE.md
@CLAUDE.md
@api/main.py
@tests/test_endpoints.py
@tests/unit/test_security_gates.py

<interfaces>
<!-- Pydantic models currently defined in api/main.py (lines 151–185). -->
<!-- Executor MUST copy these verbatim into api/schemas.py, preserving docstrings, -->
<!-- validators, typing, and decorators exactly. -->

From api/main.py:151–153:
```python
class LoginRequest(BaseModel):
    username: str
    password: str
```

From api/main.py:155–185:
```python
class SearchRequest(BaseModel):
    query: str
    mode: str = "automated"
    modules: list[str] = []
    spiderfoot_mode: str = "passive"

    @field_validator("query")
    @classmethod
    def sanitize_query(cls, v: str) -> str:
        v = v.strip()
        if not v:
            raise ValueError("Query cannot be empty")
        if len(v) < 2:
            raise ValueError("Query too short (min 2 chars)")
        if len(v) > 256:
            raise ValueError("Query too long (max 256 chars)")
        v = re.sub(r"[\x00-\x08\x0b\x0c\x0e-\x1f\x7f]", "", v)
        v = re.sub(r"[;\x27\x22\x5c]", "", v)
        return v

    @field_validator("mode")
    @classmethod
    def validate_mode(cls, v: str) -> str:
        return v if v in ("automated", "manual") else "automated"

    @field_validator("spiderfoot_mode")
    @classmethod
    def validate_sf_mode(cls, v: str) -> str:
        return v if v in ("passive", "footprint", "investigate") else "passive"
```

Required external imports inside `api/schemas.py` (nothing from api/* or modules/*):
```python
import re
from pydantic import BaseModel, field_validator
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Create api/schemas.py with LoginRequest + SearchRequest</name>
  <files>api/schemas.py</files>
  <read_first>
    - .planning/phases/15-refactor-main-py-layers/15-CONTEXT.md (D-01, D-03 Passo 1, Import Contract table — schemas is LEAF)
    - api/main.py lines 151–185 (the two Pydantic classes to lift, verbatim)
    - CLAUDE.md (Regra 3: input validation lives in backend; Regra 4: no placeholders)
  </read_first>
  <action>
    Create a brand-new file `api/schemas.py`. It MUST contain:

    1. Module docstring: `"""Pydantic I/O schemas for the FastAPI app. Leaf module — imports nothing from api/* or modules/*."""`
    2. Imports (exactly these two lines — nothing else from the project):
       - `import re`
       - `from pydantic import BaseModel, field_validator`
    3. `class LoginRequest(BaseModel):` with fields `username: str`, `password: str` — copied verbatim from main.py:151–153.
    4. `class SearchRequest(BaseModel):` with fields `query: str`, `mode: str = "automated"`, `modules: list[str] = []`, `spiderfoot_mode: str = "passive"`, and all three `@field_validator` methods (`sanitize_query`, `validate_mode`, `validate_sf_mode`) copied verbatim from main.py:155–185. Preserve `@classmethod`, regex strings, and exact `ValueError` messages.

    Do NOT add, remove, rename, or reorder any field. Do NOT change regex patterns. Do NOT add a repository layer, a factory, or sibling models. D-01 locks the structure.

    Import contract check (per CONTEXT.md "Contrato de Imports"): schemas.py is a leaf — NO imports from `api.*` or `modules.*`. Only `re` and `pydantic`.
  </action>
  <verify>
    <automated>python -c "from api.schemas import LoginRequest, SearchRequest; m = SearchRequest(query='test@example.com'); assert m.mode == 'automated'; assert m.spiderfoot_mode == 'passive'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - File exists: `test -f api/schemas.py` exits 0
    - Exactly 2 Pydantic classes: `grep -c '^class ' api/schemas.py` equals 2
    - `grep -c 'class LoginRequest(BaseModel):' api/schemas.py` equals 1
    - `grep -c 'class SearchRequest(BaseModel):' api/schemas.py` equals 1
    - All 3 validators present: `grep -c '@field_validator' api/schemas.py` equals 3
    - Import contract: `grep -E '^(from|import) (api|modules)' api/schemas.py` returns zero matches (exit code 1 from grep)
    - Only external imports: `grep -E '^(from|import) ' api/schemas.py | sort -u` yields exactly `import re` and `from pydantic import BaseModel, field_validator`
    - Importable: `python -c "from api.schemas import LoginRequest, SearchRequest"` exits 0
    - Validator still rejects empty query: `python -c "from api.schemas import SearchRequest; import pydantic; 
try: SearchRequest(query=''); raise SystemExit(1)
except pydantic.ValidationError: print('OK')"` prints OK
  </acceptance_criteria>
  <done>
    `api/schemas.py` exists as a leaf module with `LoginRequest` and `SearchRequest` (including all 3 validators), importable from a fresh Python process, with zero imports from `api.*` or `modules.*`.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Remove Pydantic classes from api/main.py and re-import from api.schemas</name>
  <files>api/main.py</files>
  <read_first>
    - .planning/phases/15-refactor-main-py-layers/15-CONTEXT.md (D-03 Passo 1, Definition of Done — monotonic decrease of `from api.main import`)
    - api/main.py (current state — lines 149–185 hold the classes to delete, lines 648–651 / 781–786 / 1559–1564 use them)
    - api/schemas.py (just created — target of the new import)
  </read_first>
  <action>
    Modify `api/main.py` in place:

    1. DELETE the class definitions at lines 151–185 inclusive (the `# ── Models ──` section containing `class LoginRequest(BaseModel):` and `class SearchRequest(BaseModel):` with all three validators). Do NOT delete the `# ── Models ──` comment line itself yet — leave it or replace with a one-line pointer, executor's choice; but the two classes must go.

    2. ADD a new import line in the imports block (near existing `from api.db import db as _db`, line 52), before `from api.orchestrator import ...`:
       `from api.schemas import LoginRequest, SearchRequest`

    3. Verify no other Pydantic models remain defined inline in main.py. The only BaseModel subclasses that should exist in main.py after this step are ZERO (SecurityHeadersMiddleware is not a BaseModel, it's a BaseHTTPMiddleware — leave it).

    4. Existing call sites (lines 650 `body: LoginRequest`, 785 `req: SearchRequest`, 805 `req: SearchRequest`) MUST work unchanged because the names are now imported, not defined. DO NOT modify the endpoint signatures.

    5. Evaluate the `from pydantic import BaseModel, ValidationError, field_validator` import at line 43. After deletion:
       - `BaseModel` is no longer needed in main.py → remove from the import.
       - `field_validator` is no longer needed in main.py → remove from the import.
       - `ValidationError` IS still needed (line 1280 catches `ValidationError` from `SpiderFootTarget`) → keep it.
       Resulting line: `from pydantic import ValidationError`

    6. Evaluate `import re` at line 17. Still needed for `detect_type`, `_validate_id`, `_sanitize_query`-style guards elsewhere (e.g. lines 710–714, 1426, 1459, 1629, 979). KEEP `import re`.
  </action>
  <verify>
    <automated>python -c "from api.main import app, LoginRequest, SearchRequest; assert LoginRequest.__module__ == 'api.schemas'; assert SearchRequest.__module__ == 'api.schemas'; print('OK')"</automated>
  </verify>
  <acceptance_criteria>
    - No inline Pydantic models in main.py: `grep -c '^class .*BaseModel' api/main.py` equals 0
    - No local `@field_validator` in main.py: `grep -c '@field_validator' api/main.py` equals 0
    - Re-import in place: `grep -c 'from api.schemas import LoginRequest, SearchRequest' api/main.py` equals 1
    - Pydantic import trimmed: `grep 'from pydantic import' api/main.py` returns exactly one line matching `from pydantic import ValidationError`
    - BaseModel no longer imported by main.py: `grep -E '^from pydantic.*BaseModel' api/main.py` returns zero matches
    - field_validator no longer imported by main.py: `grep -E '^from pydantic.*field_validator' api/main.py` returns zero matches
    - Back-compat re-export preserved (test_endpoints.py does `from api.main import _create_token, _decode_token`; tests must still pass without being rewritten in this step — validators were not previously re-exported from main.py tests, so no test rewrites needed here)
    - `python -c "import api.main"` exits 0
    - `python -c "from api.main import LoginRequest, SearchRequest"` exits 0 (FastAPI TestClient and tests relying on main-level names continue to work via the new import)
    - Monotonic decrease check: baseline `grep -rn "from api.main import" tests/ | wc -l` is recorded BEFORE task 1 and compared AFTER task 2; the AFTER value MUST be <= BEFORE value (this step introduces no new `from api.main import` references)
  </acceptance_criteria>
  <done>
    `api/main.py` no longer defines any Pydantic model; it imports `LoginRequest` and `SearchRequest` from `api.schemas`. `BaseModel` and `field_validator` are no longer imported in `main.py`. All endpoint signatures that used these types still compile.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Run the 62/62 test gate and smoke the container</name>
  <files>(none — verification only)</files>
  <read_first>
    - .planning/phases/15-refactor-main-py-layers/15-CONTEXT.md (Definition of Done per step — items 1–5)
    - CLAUDE.md (Deploy section — do NOT scp to VPS at this step; local smoke only)
  </read_first>
  <action>
    Execute the full gate required by CONTEXT.md "Definition of Done (por passo)":

    1. `pytest -q` — MUST exit 0, report 62 passed, 0 failed. If any test imports `LoginRequest` or `SearchRequest` from `api.main`, Task 2 already preserved that via re-import, so tests should be unaffected. If a pytest cache issue appears, run `pytest --cache-clear -q` once.

    2. `docker compose build` — MUST exit 0. Confirm image layers rebuild clean.

    3. `docker compose up -d` — container starts. Wait up to 15 seconds for healthcheck.

    4. `curl -fsS http://localhost:8000/health` — MUST return HTTP 200 with JSON containing `"version":"3.0.0"`.

    5. Smoke login: `curl -fsS -c /tmp/nx.cookie -X POST http://localhost:8000/api/login -H "Content-Type: application/json" -d '{"username":"admin","password":"<APP_PASSWORD from .env>"}'` returns `{"ok":true, ...}` with a `nx_session` cookie set.

    6. Smoke search: `curl -fsS -b /tmp/nx.cookie -X POST http://localhost:8000/api/search -H "Content-Type: application/json" -d '{"query":"test@example.com","mode":"automated","modules":[]}' -N` — SSE stream opens, first event has `"type":"start"`. Abort stream after first event (Ctrl+C is acceptable in interactive; in non-interactive use `--max-time 3`).

    7. `docker compose down` to clean up.

    8. Re-count `from api.main import` across the repo (tests, docs, phase plans) and confirm no new references were introduced by this step.
  </action>
  <verify>
    <automated>pytest -q 2>&1 | tail -5 | grep -E '62 passed'</automated>
  </verify>
  <acceptance_criteria>
    - `pytest -q` exit code: 0
    - pytest output contains "62 passed" (exact count)
    - `docker compose build` exit code: 0
    - After `docker compose up -d`, `curl -fsS http://localhost:8000/health` exit code: 0 within 15s
    - `/health` JSON parseable and contains `"version"` and `"status"` keys
    - Login POST returns HTTP 200
    - Search POST (authenticated) returns HTTP 200 and first SSE event type is `"start"`
    - Final `grep -rn "from api.main import" tests/ | wc -l` is equal to or less than the baseline captured in Task 2
    - `docker compose down` exits 0
  </acceptance_criteria>
  <done>
    Entire Definition of Done (CONTEXT.md items 1–6) passes at Step 1 boundary. Step 2 is unblocked.
  </done>
</task>

</tasks>

<verification>
Phase 15 Step 1 exit checks:
- `pytest -q` = 62 passed, 0 failed
- `docker compose build` exit 0
- `docker compose up` healthy < 15s; `/health` → 200
- Smoke: login 200 + search first-event 200
- `grep -rn "from api.main import" . | wc -l` monotonically non-increasing vs baseline
- `api/schemas.py` is a LEAF: `grep -E '^(from|import) (api|modules)' api/schemas.py` has zero matches
</verification>

<success_criteria>
- `api/schemas.py` exists, exports `LoginRequest` and `SearchRequest`
- `api/main.py` contains zero inline `BaseModel` subclasses and imports the two from `api.schemas`
- 62/62 tests green; container healthy; smoke flow passes
- Ready to proceed to Step 2 (`15-02-PLAN.md` — deps.py extraction)
</success_criteria>

<output>
After completion, create `.planning/phases/15-refactor-main-py-layers/15-01-SUMMARY.md` documenting:
- Exact line ranges removed from main.py
- New file footprint (api/schemas.py line count)
- pytest result and docker smoke result
- Baseline-vs-final `from api.main import` reference counts
</output>
