---
phase: 22-repository-layer-switch-code-audit-pass-2
plan: 01
type: execute
wave: 1
files_modified:
  - api/config.py
  - api/db.py
  - api/deps.py
  - api/routes/admin.py
  - api/routes/health.py
  - api/services/auth_service.py
  - api/services/search_service.py
  - tests/test_db_abstraction.py
  - tests/test_health.py
requirements: [DBM-24, DBM-25, DBM-26, DBM-27, DBM-28, DBM-29, DBM-30, DBM-31]
---

# Plan 22-01 - asyncpg Driver Swap

## Objective

Run Nexus against Postgres through `asyncpg.Pool` and update SQL to Postgres syntax.

## Tasks

1. Add `DATABASE_URL` config and pool lifecycle.
2. Rewrite `api/db.py` on asyncpg.
3. Update all application SQL placeholders and conflict clauses.
4. Store `modules_run` as `TEXT[]` and `success` as boolean.
5. Add DB pool health stats to `/health`.
6. Verify no `aiosqlite`, `_writer_loop`, `asyncio.Queue`, `?` placeholders, or `INSERT OR` remain in runtime DB paths.

