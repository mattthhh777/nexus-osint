---
phase: 16-sherlock-false-positive-filter-thordata-proxy-integration
plan: 04
type: execute
wave: 4
depends_on:
  - 16-01
  - 16-02
  - 16-03
files_modified:
  - static/js/render.js
  - static/css/meridian.css
  - tests/integration/test_phase16_e2e.py
  - .planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-NEGATIVE-MARKERS-AUDIT.md
autonomous: true
requirements:
  - PHASE16-FRONTEND-STATE-RENDER
  - PHASE16-LIKELY-BADGE
  - PHASE16-NEGATIVE-MARKERS-AUDIT
  - PHASE16-E2E-VERIFICATION
requirements_addressed:
  - PHASE16-FRONTEND-STATE-RENDER
  - PHASE16-LIKELY-BADGE
  - PHASE16-NEGATIVE-MARKERS-AUDIT
  - PHASE16-E2E-VERIFICATION
must_haves:
  truths:
    - "render.js consumes p.state directly from SSE; never recomputes state from p.confidence (D-14, D-H1)"
    - "Cards with state == 'confirmed' render with full Amber accent (existing baseline)"
    - "Cards with state == 'likely' render with muted color + 'Unverified' badge"
    - "likely items render in same grid as found items (single combined view), confirmed cards sorted before likely"
    - "found_count badge displays confirmed count; small inline indicator surfaces likely_count when > 0"
    - "module_error event with error='budget_exceeded' renders inline message 'Sherlock budget exceeded — try tomorrow' (no leak of retry_after seconds raw)"
    - "module_error event with error='invalid_username' renders inline message 'Invalid username format' (no input echo)"
    - "negative_markers manual-validation pass complete: spot-check 5 platforms with real 404 pages → markers actually match observed text"
    - "tests/integration/test_phase16_e2e.py covers full SSE round-trip with mocked sherlock_wrapper"
    - "Brand Amber/Noir palette respected — likely state uses muted variant of existing tokens, no new color introduced"
    - "No console.error / unhandled-rejection on healthy or error SSE paths"
  artifacts:
    - path: "static/js/render.js"
      provides: "renderSocial extended for state/confidence/likely + Unverified badge"
      exports: ["renderSocial"]
    - path: "static/css/meridian.css"
      provides: ".social-card--likely + .social-card-badge--unverified styles using existing Amber/Noir tokens"
    - path: ".planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-NEGATIVE-MARKERS-AUDIT.md"
      provides: "Audit log of manual validation per platform: marker text observed vs Plan-02 inlined value, with PASS/REVISE per platform"
    - path: "tests/integration/test_phase16_e2e.py"
      provides: "End-to-end SSE round-trip tests covering Phase 16 contract"
  key_links:
    - from: "static/js/render.js"
      to: "SSE event 'sherlock' contract from Plan 03"
      via: "event.found[i].state + event.likely[i] consumption"
      pattern: "\\.state|\\.likely|\\.proxy_used"
    - from: "static/css/meridian.css"
      to: "existing Amber/Noir CSS custom properties"
      via: "var(--color-*) tokens — no hardcoded hex"
      pattern: "var\\(--color-"
---

<objective>
Land Phase 16 user-visible surface and close verification loop:

1. Frontend state rendering — `renderSocial(s)` consumes `state` directly (D-H1: never recompute thresholds in JS); `confirmed` cards baseline; `likely` cards muted + `Unverified` badge.
2. CSS additions — `.social-card--likely` and `.social-card-badge--unverified` from existing Amber/Noir tokens. No new palette.
3. module_error handling — surface `budget_exceeded` and `invalid_username` SSE events as friendly messages (no raw retry_after seconds, no input echo).
4. negative_markers manual validation — spot-check 5 platforms (GitHub, Reddit, X, LinkedIn, Instagram); document observed-vs-planned. Update PLATFORMS where reality disagrees.
5. End-to-end integration tests — full SSE round-trip with mocked `search_username`.

Output: 2 frontend files modified, 1 audit doc new, 1 integration test file new (~150 LOC).
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/STATE.md
@.planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-CONTEXT.md
@.planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-RESEARCH.md
@.planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-01-PLAN.md
@.planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-02-PLAN.md
@.planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-03-PLAN.md
@CLAUDE.md
@static/js/render.js
@static/css/meridian.css

<interfaces>
SSE 'sherlock' event contract (from Plan 03):
- type: "sherlock"
- found_count: number (confirmed only)
- likely_count: number
- total_checked: number
- source: string
- proxy_used: boolean
- found: list of {platform, url, category, icon, state="confirmed", confidence}
- likely: list of {platform, url, category, icon, state="likely", confidence}

SSE 'module_error' event:
- {type, module="sherlock", error="invalid_username"}
- {type, module="sherlock", error="budget_exceeded", retry_after=86400}
- {type, module="sherlock", error="Sherlock timed out after 60s — partial results unavailable"}

New CSS classes (built from existing Amber/Noir tokens):
- .social-card--likely (muted color, reduced opacity)
- .social-card-badge (base badge style)
- .social-card-badge--unverified (muted variant)
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Negative-markers manual validation audit</name>
  <files>.planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-NEGATIVE-MARKERS-AUDIT.md, modules/sherlock_wrapper.py (only if revisions needed)</files>
  <read_first>
    - modules/sherlock_wrapper.py (post-Plan-02 — confirms 25 platforms with negative_markers inlined)
    - .planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-RESEARCH.md ("Negative Markers Per Platform" table)
    - .planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-CONTEXT.md (D-09)
    - CLAUDE.md (regra 2 — research is starting point, must be validated)
  </read_first>
  <action>
    Manually validate negative markers for 5 representative platforms via known-nonexistent username (e.g. `nx_fp_check_zzz_999`):

    Platform | Test URL pattern | Expected marker (Plan-02)
    ---|---|---
    GitHub | `https://github.com/nx_fp_check_zzz_999` | "Page not found"
    Reddit | `https://www.reddit.com/user/nx_fp_check_zzz_999` | "page not found"
    X / Twitter | `https://twitter.com/nx_fp_check_zzz_999` | "doesn't exist"
    LinkedIn | `https://linkedin.com/in/nx_fp_check_zzz_999` | (verify — likely 999/login wall)
    Instagram | `https://instagram.com/nx_fp_check_zzz_999` | (verify — likely 404 / login wall)

    Use one-shot Python REPL or temporary script (NOT committed) routed through Thordata if `THORDATA_PROXY_URL` set; otherwise direct. Per platform:
    1. Capture status code + first 256KB body.
    2. Compare against marker inlined in `modules/sherlock_wrapper.py` PLATFORMS dict.
    3. Record observation in `16-NEGATIVE-MARKERS-AUDIT.md`.

    Audit doc structure:
    - Title: "Phase 16 — Negative Markers Manual Validation Audit"
    - Date validated, validator
    - Method paragraph (one paragraph)
    - Per-platform sections (5): planned marker, observed body excerpt (first 200 chars), status code, verdict (PASS/REVISE), action taken
    - Summary: PASS count, REVISE count, PLATFORMS dict patches list, note on remaining 20 platforms

    If spot-check disagrees, update `modules/sherlock_wrapper.py` PLATFORMS entry inline AND note in audit doc. At most 5 platforms touched.

    Bandwidth cost: 5 × ~200KB = ~1MB through Thordata. One-time only; do NOT repeat in tests (mocked).
  </action>
  <verify>
    <automated>cd C:/Users/vtbit/Documents/nexus_osint && test -f .planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-NEGATIVE-MARKERS-AUDIT.md && grep -c "PASS\|REVISE" .planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-NEGATIVE-MARKERS-AUDIT.md</automated>
  </verify>
  <acceptance_criteria>
    - `16-NEGATIVE-MARKERS-AUDIT.md` exists with all 5 platform sections.
    - Each section has status code, body excerpt, verdict, action.
    - REVISE → corresponding PLATFORMS entry updated; documented.
    - At least 5 PASS/REVISE verdicts.
    - If PLATFORMS modified → `python -m pytest tests/unit/test_sherlock_wrapper.py -x -q` still passes.
  </acceptance_criteria>
  <done>
    Manual validation completed for 5 platforms; audit doc records observations; PLATFORMS dict updated where research disagreed.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: renderSocial extension for state + Unverified badge + likely list</name>
  <files>static/js/render.js, static/css/meridian.css</files>
  <read_first>
    - static/js/render.js (current `renderSocial` lines 476-539)
    - static/css/meridian.css (existing `.social-card*` classes)
    - .planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-CONTEXT.md (D-13, D-14, D-H1)
    - CLAUDE.md (regra 3 — frontend reads, never decides; Amber/Noir non-negotiable)
  </read_first>
  <action>
    A. Modify `renderSocial(s)` in `static/js/render.js`:
    1. Build unified `items` array: `[...s.found, ...(s.likely || [])]`.
    2. Sort items so `confirmed` renders before `likely`.
    3. Per item, branch markup on `p.state`:
       - `state === "confirmed"` → existing card markup unchanged.
       - `state === "likely"` → same markup PLUS `class="social-card social-card--likely"` AND `<span class="social-card-badge social-card-badge--unverified">Unverified</span>` in header next to platform name.
    4. Top-of-panel badge stays as `s.found_count` (confirmed-only). Add inline indicator next to panel title when `s.likely_count > 0`: `<span class="panel-likely-count">+${s.likely_count} unverified</span>`.
    5. Empty-state condition: `!s || ((s.found?.length || 0) + (s.likely?.length || 0)) === 0`.
    6. Filter input threshold: `items.length > 10`.
    7. NEVER recompute `state` from `p.confidence` (D-H1).
    8. NEVER reference `p.negative_markers` or `*_pts` — not in payload (D-H2/D-H3).

    B. Add `module_error` rendering branch. Locate SSE dispatcher (`grep -n module_error static/js/`). Add handling for `event.module === "sherlock"`:
    - `error === "invalid_username"` → "Invalid username format"
    - `error === "budget_exceeded"` → "Sherlock budget exceeded — try tomorrow" (NEVER echo raw `retry_after` seconds)
    - other → existing behavior

    C. Add CSS to `static/css/meridian.css` (append after existing `.social-card-*` block):

```css
/* Phase 16: likely state — muted variant of confirmed cards */
.social-card--likely {
  opacity: 0.72;
  border-color: var(--color-border-muted, var(--color-border));
}

.social-card-badge {
  display: inline-flex;
  align-items: center;
  padding: 2px 8px;
  margin-left: 8px;
  border-radius: 999px;
  font-family: var(--font-data);
  font-size: 0.65rem;
  letter-spacing: 0.04em;
  text-transform: uppercase;
}

.social-card-badge--unverified {
  background: var(--color-amber-bg-subtle, rgba(217, 159, 25, 0.08));
  color: var(--color-text-tertiary);
  border: 1px solid var(--color-border-muted, var(--color-border));
}

.panel-likely-count {
  margin-left: 6px;
  font-family: var(--font-data);
  font-size: 0.7rem;
  color: var(--color-text-tertiary);
}
```

    Use ONLY existing custom properties. Fallback values inside `var()` accommodate tokens not yet defined.

    D. CSP — class additions only, no inline styles. Verify no inline `style=` attributes added beyond existing.
  </action>
  <verify>
    <automated>cd C:/Users/vtbit/Documents/nexus_osint && grep -n "social-card--likely\|social-card-badge--unverified" static/css/meridian.css && grep -n "p\\.state\\|state === " static/js/render.js</automated>
  </verify>
  <acceptance_criteria>
    - `grep -nE "p\\.state\\b" static/js/render.js` returns at least 2 matches.
    - `grep -nE "p\\.confidence\\s*[<>=]" static/js/render.js` returns ZERO matches (D-H1).
    - `grep -n "p\\.negative_markers\\|status_pts\\|text_pts\\|size_pts" static/js/render.js` returns ZERO matches.
    - `grep -n "social-card--likely" static/js/render.js` returns at least one match.
    - `grep -n "social-card-badge--unverified" static/js/render.js` returns at least one match.
    - `grep -n "Unverified" static/js/render.js` returns at least one match.
    - `grep -n "social-card--likely" static/css/meridian.css` returns at least one match.
    - `grep -n "social-card-badge--unverified" static/css/meridian.css` returns at least one match.
    - `git diff static/css/meridian.css | grep -E "^\\+.*#[0-9a-fA-F]{3,6}\\b"` ZERO matches (no NEW hex).
    - `grep -rn "budget_exceeded\\|invalid_username" static/js/` returns at least 2 matches.
    - Any `retry_after` reference must produce human-friendly relative message, NEVER raw seconds.
  </acceptance_criteria>
  <done>
    renderSocial consumes state directly without recomputing; likely cards render muted with Unverified badge; module_error events surface friendly messages; CSS uses Amber/Noir tokens only.
  </done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: End-to-end integration tests for Phase 16 contract</name>
  <files>tests/integration/test_phase16_e2e.py</files>
  <read_first>
    - tests/integration/test_phase16_routes.py (post-Plan-03 — reuse fixtures)
    - api/services/search_service.py (post-Plan-03)
    - modules/sherlock_wrapper.py (post-Plan-02)
    - 16-CONTEXT.md (D-13, D-14, D-H1, D-H2, D-H3, D-H12, D-H13)
  </read_first>
  <behavior>
    - Test 1 (confirmed only): mock returns 3 found, 0 likely → SSE `found_count=3 likely_count=0 likely=[]`.
    - Test 2 (mixed): 2 found + 3 likely → SSE `found_count=2 likely_count=3` and both arrays populated.
    - Test 3 (proxy_used true): mock returns `proxy_used=True` → SSE reflects.
    - Test 4 (budget exceeded): monkeypatch `_budget._bytes_today` over hard limit → module_error with `error="budget_exceeded"` and `retry_after=86400`. Sherlock NOT invoked.
    - Test 5 (invalid username): SSE search with query containing `/` → module_error `error="invalid_username"`. Sherlock NOT invoked.
    - Test 6 (D-H2/D-H3 audit): inspect raw SSE payload bytes; assert no occurrence of `negative_markers`, `status_pts`, `text_pts`, `size_pts`, `confidence_breakdown` substrings.
    - Test 7 (D-H13 audit log): capture loguru output; SSE search completes; log line contains `username_hash=` AND does NOT contain plaintext username.
    - Test 8 (PlatformResult serialization tightness): each item in `event.found` and `event.likely` has EXACTLY keys `{platform, url, category, icon, state, confidence}`.
  </behavior>
  <action>
    Create `tests/integration/test_phase16_e2e.py`. Use FastAPI `TestClient` + monkeypatch + loguru capture. Build stub `SherlockResult` fixture exercising full serializer path.

    Stub pseudocode:
    ```python
    from modules.sherlock_wrapper import PlatformResult, SherlockResult

    def build_sherlock_stub(found_count: int, likely_count: int, proxy_used: bool = False) -> SherlockResult:
        return SherlockResult(
            username="alice42",
            success=True,
            found=[
                PlatformResult(
                    platform=f"P{i}", url=f"https://p{i}.example/alice42",
                    category="Test", icon="🧪",
                    state="confirmed", confidence=85, found=True,
                ) for i in range(found_count)
            ],
            likely=[
                PlatformResult(
                    platform=f"L{i}", url=f"https://l{i}.example/alice42",
                    category="Test", icon="🧪",
                    state="likely", confidence=55, found=True,
                ) for i in range(likely_count)
            ],
            not_found=[], errors=[],
            source="internal",
            proxy_used=proxy_used,
        )
    ```

    Reuse fixture pattern from `tests/integration/test_phase16_routes.py` — don't duplicate auth setup.
  </action>
  <verify>
    <automated>cd C:/Users/vtbit/Documents/nexus_osint && python -m pytest tests/integration/test_phase16_e2e.py -x -q --tb=short</automated>
  </verify>
  <acceptance_criteria>
    - File `tests/integration/test_phase16_e2e.py` exists with at least 8 test functions.
    - `python -m pytest tests/integration/test_phase16_e2e.py -x -q` exits 0.
    - Full Phase 16 test suite green: `python -m pytest tests/unit/test_budget.py tests/unit/test_sherlock_wrapper.py tests/unit/test_schemas_phase16.py tests/integration/test_phase16_routes.py tests/integration/test_phase16_e2e.py -x -q --tb=short` exits 0.
    - Pre-existing test suite still green.
  </acceptance_criteria>
  <done>
    8 E2E tests cover full Phase 16 SSE contract; D-H2/D-H3 leakage absence verified at SSE-bytes level; D-H13 audit log assertion green.
  </done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Manual UI smoke test + deploy plan</name>
  <files>(no code changes — manual verification + deploy log)</files>
  <read_first>
    - CLAUDE.md ("DEPLOY — VPS PRODUCTION" — scp + docker compose pattern)
    - 16-CONTEXT.md (D-07 — non-blocking startup)
  </read_first>
  <action>
    1. Local smoke — `uvicorn api.main:app --host 127.0.0.1 --port 8000`; browser; search real username:
       - Confirm at least one card renders full Amber styling (state=confirmed).
       - Confirm at least one card renders muted with Unverified badge (state=likely) — if none appear, lower `SHERLOCK_LIKELY_THRESHOLD=20` temporarily via env.
       - Confirm proxy_used reflected (if exposed).
       - DevTools console zero errors.
       - `/health` (admin) shows `thordata` sub-object.
    2. Deploy — only after Phase 16 plans 01-04 merged to master:
       ```bash
       scp -r api/ modules/ static/ root@146.190.142.50:/root/nexus-osint/
       scp .env.example root@146.190.142.50:/root/nexus-osint/
       ssh root@146.190.142.50 "cd /root/nexus-osint && docker compose up -d --build"
       ssh root@146.190.142.50 "docker logs nexus-osint-nexus-1 --tail 30"
       ```
       Verify lifespan log line `"Thordata proxy OK | proxy=t.pr.thordata.net:9999 exit_ip=..."` appears.
    3. Production smoke — issue search via live UI; verify behavior matches local.
    4. Rollback path — if proxy startup logs show repeated WARNING and Sherlock returns mostly errors:
       - Set `THORDATA_PROXY_URL=` (empty) in VPS `.env`; `docker compose restart`.
       - App reverts to direct DO IP path.
       - File follow-up on `.planning/STATE.md`.
  </action>
  <verify>
    <automated>echo "Manual task — observational verification; no automated check"</automated>
  </verify>
  <acceptance_criteria>
    - Local smoke checklist completed (Claude reports observed behavior).
    - Deploy executed only after master green.
    - VPS `docker logs` shows Thordata startup line.
    - No 500 errors on first 5 production searches.
  </acceptance_criteria>
  <done>
    Local smoke confirms Phase 16 behavior; production deploy succeeds with proxy startup logged; rollback path documented.
  </done>
</task>

</tasks>

<verification>
- All Phase 16 tests green: `python -m pytest tests/unit/test_budget.py tests/unit/test_sherlock_wrapper.py tests/unit/test_schemas_phase16.py tests/integration/test_phase16_routes.py tests/integration/test_phase16_e2e.py -x -q --tb=short`.
- Full project test suite green: `python -m pytest -x -q --tb=short`.
- `16-NEGATIVE-MARKERS-AUDIT.md` exists with 5 platforms validated.
- D-H1 audit: `grep -rE "(p|item|res)\\.confidence\\s*[<>=]" static/js/` ZERO matches.
- D-H2/D-H3 audit: `grep -rE "negative_markers|status_pts|text_pts|size_pts|confidence_breakdown" static/js/ static/css/` ZERO matches.
- Brand audit: `git diff static/css/meridian.css | grep -E "^\\+.*#[0-9a-fA-F]{3,6}\\b"` ZERO matches.
- Manual UI smoke: confirmed + likely cards render distinctly; module_error events surface friendly messages.
</verification>

<success_criteria>
- renderSocial consumes state directly (D-H1 enforced); likely cards visually distinct; Unverified badge present.
- module_error for sherlock surfaces friendly messages.
- Negative markers manually validated for 5 platforms; PLATFORMS dict reflects reality.
- 8+ E2E tests cover full Phase 16 SSE contract.
- Full project test suite green.
- VPS deploy completed; lifespan startup log shows proxy state.
- Brand Amber/Noir preserved.
</success_criteria>

<output>
After completion, create `.planning/phases/16-sherlock-false-positive-filter-thordata-proxy-integration/16-04-SUMMARY.md` documenting:
- Frontend changes: renderSocial state-branched, Unverified badge, muted likely cards
- CSS additions: 3 new classes from existing tokens
- module_error UX: budget_exceeded → "try tomorrow", invalid_username → "Invalid format"
- Negative-markers audit: PASS/REVISE per platform
- 8 E2E tests; full Phase 16 test count summary
- Deploy log: VPS rebuild + smoke results
- Phase 16 closing: ROADMAP.md + STATE.md mark Phase 16 COMPLETE
</output>
